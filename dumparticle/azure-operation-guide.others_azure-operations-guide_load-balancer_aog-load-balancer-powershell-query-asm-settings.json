{"Title":"使用 PowerShell 查看经典模式下的负载均衡配置","Description":"通过 PowerShell 脚本程序查看经典模式下的负载均衡配置","Content":"\r # 使用 PowerShell 查看经典部署模式下的负载均衡配置  \r \r 在 Azure 服务管理（ ASM ） 模式中，负载均衡是通过更新虚拟机来设置的。查询负载均衡，只能通过一台台虚拟机进行查询。如果订阅中的虚拟机数量众多，查询起来也会相当费力。为此通过以下 PowerShell 脚本程序能够一次性查询出当前订阅下所有 ASM 模式中配置的负载均衡设置。  \r \r ```\r $cs= get-azurevm\r $n=0\r while ( $n -lt $cs.Count )\r {\r  $ep = $cs[$n].VM | Get-AzureEndpoint\r  $m=0\r  $lb=0\r  while ($m -lt $ep.count)\r  {\r     if ($ep[$m].LBSetName)\r         {\r             $lb = 1\r         }\r     $m = $m + 1\r  }\r  $m=0\r  if ($lb -eq 1)\r  {\r     echo -------------------------------\r     $vmname = \"VMName: \"\r     $DNSName = \"DNSName: \"\r     $vmname + $cs[$n].Namec\r     $DNSName + $cs[$n].DNSName\r     echo \"    \"\r     while ($m -lt $ep.count)\r     {\r         if ($ep[$m].LBSetName)\r         {\r             $ep[$m]\r         }\r         $m = $m + 1\r     }\t\r  }\r  $n = $n + 1\r }\r ```\r \r 将上面脚本保存为 `.ps1` 的后缀名。  \r 登录到 AzureChinaCloud，如果有多个订阅，请将要查询的订阅设置为当前订阅。\r \r ![subscriptions](./media/aog-load-balancer-powershell-query-asm-settings/subscriptions.png)\r \r 运行脚本。  \r 一个示例输出如下，其中包含了内部负载均衡，外部负载均衡。如果虚拟机处于关机状态，则 VIP 会显示为空。\r \r ![subscriptions](./media/aog-load-balancer-powershell-query-asm-settings/load-balancer-settings.png)"}