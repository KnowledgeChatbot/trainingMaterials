{"Title":"Azure CLI 脚本示例 - 使用批处理运行作业","Description":"Azure CLI 脚本示例 - 使用批处理运行作业","Content":"# <a name=\"running-jobs-on-azure-batch-with-azure-cli\"></a>使用 Azure CLI 在 Azure Batch 上运行作业\r \r 此脚本将创建一个批处理作业，并将一系列任务添加到该作业。 它还演示了如何监视作业及其任务。 最后，它演示如何有效地查询 Batch 服务，以获取有关作业任务的信息。\r \r ## <a name=\"prerequisites\"></a>先决条件\r \r - 按照 [Azure CLI 安装指南](/cli/install-azure-cli)中提供的说明安装 Azure CLI（如果尚未这样做）。\r - 创建 Batch 帐户（如果还没有帐户）。 有关创建帐户的示例脚本，请参阅[使用 Azure CLI 创建 Batch 帐户](/batch/scripts/batch-cli-sample-create-account/)。\r - 将应用程序配置为从启动任务运行（如果尚未这样做）。 有关用于创建应用程序并将应用程序包上传到 Azure 的示例脚本，请参阅[使用 Azure CLI 将应用程序添加到 Azure Batch](/batch/scripts/batch-cli-sample-add-application/)。\r - 配置将在其中运行作业的池。 有关使用云服务配置或虚拟机配置创建池的示例脚本，请参阅[使用 Azure CLI 管理 Azure Batch 池](/batch/scripts/batch-cli-sample-manage-pool/)。\r \r ## <a name=\"sample-script\"></a>示例脚本\r \r ```azurecli\r #!/bin/bash\r \r # Authenticate Batch account CLI session.\r az batch account login -g myresource group -n mybatchaccount\r \r # Create a new job to encapsulate the tasks that we want to add.\r # We'll assume a pool has already been created with the ID 'mypool' - for more information\r # see the sample script for managing pools.\r az batch job create --id myjob --pool-id mypool\r \r # Now we will add tasks to the job.\r # We'll assume an application package has already been uploaded with the ID 'myapp' - for\r # more information see the sample script for adding applications.\r az batch task create \\\r     --job-id myjob \\\r     --task-id task1 \\\r     --application-package-references myapp#1.0\r     --command-line \"cmd /c %AZ_BATCH_APP_PACKAGE_MYAPP#1.0%\\\\myapp.exe\"\r \r # If we want to add many tasks at once - this can be done by specifying the tasks\r # in a JSON file, and passing it into the command. See tasks.json for formatting.\r az batch task create --job-id myjob --json-file tasks.json\r \r # Now that all the tasks are added - we can update the job so that it will automatically\r # be marked as completed once all the tasks are finished.\r az batch job set --job-id myjob --on-all-tasks-complete terminateJob\r \r # Monitor the status of the job.\r az batch job show --job-id myjob\r \r # Monitor the status of a task.\r az batch task show --job-id myjob --task-id task1\r ```\r \r ## <a name=\"clean-up-job\"></a>清除作业\r \r 运行上述示例脚本后，可运行以下命令以删除作业及其所有任务。 请注意，池将需要单独删除。 有关创建和删除池的详细信息，请参阅[使用 Azure CLI 管理 Azure Batch 池](/batch/scripts/batch-cli-sample-manage-pool/)。\r \r ```azurecli\r az batch job delete --job-id myjob\r ```\r \r ## <a name=\"script-explanation\"></a>脚本说明\r \r 此脚本使用以下命令创建批处理作业和任务。 表中的每条命令均链接到特定于命令的文档。\r \r | 命令 | 说明 |\r |---|---|\r | [az batch account login](/cli/batch/account#login) | 针对批处理帐户进行身份验证。  |\r | [az batch job create](/cli/batch/job#create) | 创建批处理作业。  |\r | [az batch job set](/cli/batch/job#set) | 更新批处理作业的属性。  |\r | [az batch job show](/cli/batch/job#show) | 检索指定批处理作业的详细信息。  |\r | [az batch task create](/cli/batch/task#create) | 将任务添加到指定的批处理作业。  |\r | [az batch task show](/cli/batch/task#show) | 从指定的批处理作业中检索任务的详细信息。  |\r | [az batch task list](/cli/batch/task#list) | 列出与指定的作业关联的任务。  |\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r 有关 Azure CLI 的详细信息，请参阅 [Azure CLI 文档](/cli/overview)。\r \r 可以在 [Azure Batch CLI 文档](../batch-cli-samples.md)中找到其他批处理 CLI 脚本示例。\r \r "}