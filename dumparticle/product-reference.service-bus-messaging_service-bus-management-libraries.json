{"Title":"Azure 服务总线管理库","Description":"通过 .NET 管理服务总线命名空间和消息实体","Content":"# <a name=\"service-bus-management-libraries\"></a>服务总线管理库\r \r Azure 服务总线管理库可以动态预配服务总线命名空间和实体。 这样可以实现复杂的部署和消息方案，并能以编程方式确定要预配的实体。 这些库目前可用于 .NET。\r \r ## <a name=\"supported-functionality\"></a>受支持的功能\r \r * 创建、更新、删除命名空间\r * 创建、更新、删除队列\r * 创建、更新、删除主题\r * 创建、更新、删除订阅\r \r ## <a name=\"prerequisites\"></a>先决条件\r \r 若要开始使用服务总线管理库，必须使用 Azure Active Directory (AAD) 服务进行身份验证。 AAD 要求身份验证为服务主体，且该主体提供对 Azure 资源的访问权限。 有关创建服务主体的信息，请参阅以下文章之一：  \r \r * [使用 Azure 门户创建可访问资源的 Active Directory 应用程序和服务主体](../azure-resource-manager/resource-group-create-service-principal-portal.md)\r * [使用 Azure PowerShell 创建服务主体来访问资源](../azure-resource-manager/resource-group-authenticate-service-principal.md)\r * [使用 Azure CLI 创建服务主体来访问资源](../azure-resource-manager/resource-group-authenticate-service-principal-cli.md)\r \r 这些教程提供 `AppId`（客户端 ID）、`TenantId` 和 `ClientSecret`（身份验证密钥），这些都将用于管理库进行的身份验证。 若要对资源组运行命令，必须拥有“所有者”权限。\r \r ## <a name=\"programming-pattern\"></a>编程模式\r \r 所有服务总线资源的操纵模式都遵循常用协议：\r \r 1. 使用 **Microsoft.IdentityModel.Clients.ActiveDirectory** 库从 Azure Active Directory 获取令牌。\r     ```csharp\r     var context = new AuthenticationContext($\"https://login.chinacloudapi.cn/{tenantId}\");\r \r     var result = await context.AcquireTokenAsync(\r         \"https://management.core.chinacloudapi.cn/\",\r         new ClientCredential(clientId, clientSecret)\r     );\r     ```\r \r 1. 创建 `ServiceBusManagementClient` 对象。\r \r     ```csharp\r     var creds = new TokenCredentials(token);\r     var sbClient = new ServiceBusManagementClient(creds)\r     {\r         SubscriptionId = SettingsCache[\"SubscriptionId\"]\r     };\r     ```\r \r 1. 将 CreateOrUpdate 参数设置为指定的值。\r \r     ```csharp\r     var queueParams = new QueueCreateOrUpdateParameters()\r     {\r         Location = SettingsCache[\"DataCenterLocation\"],\r         EnablePartitioning = true\r     };\r     ```\r \r 1. 执行调用。\r \r     ```csharp\r     await sbClient.Queues.CreateOrUpdateAsync(resourceGroupName, namespaceName, QueueName, queueParams);\r     ```\r \r ## <a name=\"next-steps\"></a>后续步骤\r * [.NET 管理示例](https://github.com/Azure-Samples/service-bus-dotnet-management/)\r * [Microsoft.Azure.Management.ServiceBus API 参考](https://docs.microsoft.com/en-us/dotnet/api/Microsoft.Azure.Management.ServiceBus)\r \r <!--Update_Description:update meta properties and wording-->"}