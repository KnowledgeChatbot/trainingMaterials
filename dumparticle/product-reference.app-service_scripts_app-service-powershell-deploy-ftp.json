{"Title":"Azure PowerShell 脚本示例 - 使用 FTP 将文件上传到 Web 应用","Description":"Azure PowerShell 脚本示例 - 使用 FTP 将文件上传到 Web 应用","Content":"# <a name=\"upload-files-to-a-web-app-using-ftp\"></a>使用 FTP 将文件上传到 Web 应用\r \r 此示例脚本使用其相关资源，在应用服务中创建 Web 应用，并使用 FTP（通过 [WebClient.UploadFile()](https://msdn.microsoft.com/library/ms144229.aspx)）部署 Web 应用代码。\r \r 必要时，请使用 [Azure PowerShell 指南](https://docs.microsoft.com/powershell/azure/overview)中的说明安装 Azure PowerShell，并运行 `Login-AzureRmAccount -EnvironmentName AzureChinaCloud` 创建与 Azure 的连接。\r \r ## <a name=\"sample-script\"></a>示例脚本\r \r ```powershell\r $appdirectory=\"<Replace with your app directory>\"\r $webappname=\"mywebapp$(Get-Random)\"\r $location=\"China North\"\r \r # Create a resource group.\r New-AzureRmResourceGroup -Name myResourceGroup -Location $location\r \r # Create an App Service plan in `Free` tier.\r New-AzureRmAppServicePlan -Name $webappname -Location $location `\r -ResourceGroupName myResourceGroup -Tier Free\r \r # Create a web app.\r New-AzureRmWebApp -Name $webappname -Location $location -AppServicePlan $webappname `\r -ResourceGroupName myResourceGroup\r \r # Get publishing profile for the web app\r $xml = (Get-AzureRmWebAppPublishingProfile -Name $webappname `\r -ResourceGroupName myResourceGroup `\r -OutputFile null)\r \r # Extract connection information from publishing profile\r $username = $xml.SelectNodes(\"//publishProfile[@publishMethod=`\"FTP`\"]/@userName\").value\r $password = $xml.SelectNodes(\"//publishProfile[@publishMethod=`\"FTP`\"]/@userPWD\").value\r $url = $xml.SelectNodes(\"//publishProfile[@publishMethod=`\"FTP`\"]/@publishUrl\").value\r \r # Upload files recursively \r Set-Location $appdirectory\r $webclient = New-Object -TypeName System.Net.WebClient\r $webclient.Credentials = New-Object System.Net.NetworkCredential($username,$password)\r $files = Get-ChildItem -Path $appdirectory -Recurse | Where-Object{!($_.PSIsContainer)}\r foreach ($file in $files)\r {\r     $relativepath = (Resolve-Path -Path $file.FullName -Relative).Replace(\".\\\", \"\").Replace('\\', '/')\r     $uri = New-Object System.Uri(\"$url/$relativepath\")\r     \"Uploading to \" + $uri.AbsoluteUri\r     $webclient.UploadFile($uri, $file.FullName)\r } \r $webclient.Dispose()\r \r ```\r \r ## <a name=\"clean-up-deployment\"></a>清理部署 \r \r 运行脚本示例后，可以使用以下命令删除资源组、Web 应用以及所有相关资源。\r \r ```powershell\r Remove-AzureRmResourceGroup -Name $webappname -Force\r ```\r \r ## <a name=\"script-explanation\"></a>脚本说明\r \r 此脚本使用以下命令。 表中的每条命令均链接到特定于命令的文档。\r \r | 命令 | 说明 |\r |---|---|\r | [New-AzureRmResourceGroup](https://docs.microsoft.com/powershell/module/azurerm.resources/new-azurermresourcegroup) | 创建用于存储所有资源的资源组。 |\r | [New-AzureRmAppServicePlan](https://docs.microsoft.com/powershell/module/azurerm.websites/new-azurermappserviceplan) | 创建应用服务计划。 |\r | [New-AzureRmWebApp](https://docs.microsoft.com/powershell/module/azurerm.websites/new-azurermwebapp) | 创建 Web 应用。 |\r | [Get-AzureRmWebAppPublishingProfile](https://docs.microsoft.com/powershell/module/azurerm.websites/get-azurermwebapppublishingprofile) | 获取 Web 应用的发布配置文件。 |\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r 有关 Azure PowerShell 模块的详细信息，请参阅 [Azure PowerShell 文档](https://docs.microsoft.com/powershell/azure/overview)。\r \r 可以在 [Azure PowerShell 示例](../app-service-powershell-samples.md)中找到 Azure 应用服务 Web 应用的其他 Azure Powershell 示例。\r "}