{"Title":"Azure 事件中心管理库","Description":"通过 .NET 管理事件中心命名空间和实体","Content":"# <a name=\"event-hubs-management-libraries\"></a>事件中心管理库\r \r 事件中心管理库可以动态设置事件中心命名空间和实体。 这样可以实现复杂的部署和消息传递方案，以便能够以编程方式确定要设置的实体。 这些库目前可用于 .NET。\r \r ## <a name=\"supported-functionality\"></a>受支持的功能\r \r * 创建、更新、删除命名空间\r * 事件中心创建、更新、删除\r * 资源组创建、更新、删除\r \r ## <a name=\"prerequisites\"></a>先决条件\r \r 若要开始使用事件中心管理库，必须使用 Azure Active Directory (AAD) 进行身份验证。 AAD 要求以提供 Azure 资源访问权限的服务主体身份进行身份验证。 有关创建服务主体的信息，请参阅以下文章之一：  \r \r * [使用 Azure 门户创建可访问资源的 Active Directory 应用程序和服务主体](../azure-resource-manager/resource-group-create-service-principal-portal.md)\r * [使用 Azure PowerShell 创建服务主体来访问资源](../azure-resource-manager/resource-group-authenticate-service-principal.md)\r * [使用 Azure CLI 创建服务主体来访问资源](../azure-resource-manager/resource-group-authenticate-service-principal-cli.md)\r \r 这些教程提供 `AppId`（客户端 ID）、`TenantId` 和 `ClientSecret`（身份验证密钥），这些都将用于管理库进行的身份验证。 若要对资源组运行命令，必须拥有“所有者”权限。\r \r ## <a name=\"programming-pattern\"></a>编程模式\r \r 所有事件中心资源的操纵模式都遵循常用协议：\r \r 1. 使用 `Microsoft.IdentityModel.Clients.ActiveDirectory` 库从 AAD 获取令牌。\r     ```csharp\r     var context = new AuthenticationContext($\"https://login.chinacloudapi.cn/{tenantId}\");\r \r     var result = await context.AcquireTokenAsync(\r         \"https://management.core.chinacloudapi.cn/\",\r         new ClientCredential(clientId, clientSecret)\r     );\r     ```\r \r 2. 创建 `EventHubManagementClient` 对象。\r \r     ```csharp\r     var creds = new TokenCredentials(token);\r     var ehClient = new EventHubManagementClient(creds)\r     {\r         SubscriptionId = SettingsCache[\"SubscriptionId\"]\r     };\r     ```\r \r 3. 将 `CreateOrUpdate` 参数设置为指定值。\r \r     ```csharp\r     var ehParams = new EventHubCreateOrUpdateParameters()\r     {\r         Location = SettingsCache[\"DataCenterLocation\"]\r     };\r     ```\r \r 4. 执行调用。\r \r     ```csharp\r     await ehClient.EventHubs.CreateOrUpdateAsync(resourceGroupName, namespaceName, EventHubName, ehParams);\r     ```\r \r ## <a name=\"next-steps\"></a>后续步骤\r * [.NET 管理示例](https://github.com/Azure-Samples/event-hubs-dotnet-management/)\r * [Microsoft.Azure.Management.EventHub 引用](https://docs.microsoft.com/dotnet/api/Microsoft.Azure.Management.EventHub)\r \r <!--Update_Description: update meta properties, wording update-->"}