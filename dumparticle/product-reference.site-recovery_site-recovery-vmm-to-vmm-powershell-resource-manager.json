{"Title":"使用 PowerShell 将 VMM 中的 Hyper-V VM 复制到辅助站点 (Azure Resource Manager)","Description":"介绍如何部署 Azure Site Recovery，以便使用 PowerShell (Resource Manager) 来协调 VMM 云中 Hyper-V VM 到辅助 VMM 站点的复制、故障转移和恢复","Content":"# <a name=\"replicate-hyper-v-virtual-machines-in-vmm-clouds-to-a-secondary-vmm-site-using-powershell-resource-manager\"></a>使用 PowerShell (Resource Manager) 将 VMM 云中的 Hyper-V 虚拟机复制到辅助 VMM 站点\r \r 本文说明设置 Azure Site Recovery 以便将 System Center VMM 云中的 Hyper-V 虚拟机复制到辅助站点中的 System Center VMM 云时，如何使用 PowerShell 自动完成常见任务。\r \r ## <a name=\"on-premises-prerequisites\"></a>本地先决条件\r 下面是在主要和辅助本地站点中部署此方案所要满足的条件：\r \r | **先决条件** | **详细信息** |\r | --- | --- |\r | **VMM** |建议在主站点和辅助站点中各部署一个 VMM 服务器。<br/><br/> 也可以在单个 VMM 服务器上的云之间复制。 为此，至少需要在 VMM 服务器上配置两个云。<br/><br/> VMM 服务器应当至少运行具有最新更新的 System Center 2012 SP1。<br/><br/> 每个 VMM 服务器上必须配置了一个或多个云，所有云中必须设置了 Hyper-V 容量配置文件。 <br/><br/>云必须包含一个或多个 VMM 主机组。 VMM 服务器应具有 Internet 访问权限。 |\r | **Hyper-V** |Hyper-V 服务器必须至少运行具有 Hyper-V 角色且安装了最新更新的 Windows Server 2012。<br/><br/> Hyper-V 服务器应包含一个或多个 VM。<br/><br/>  Hyper-V 主机服务器应位于主要和辅助 VMM 云中的主机组内。<br/><br/> 如果在 Windows Server 2012 R2 上的群集中运行 Hyper-V，应安装[更新 2961977](https://support.microsoft.com/kb/2961977)<br/><br/> 如果在 Windows Server 2012 上的群集中运行 Hyper-V，请注意，如果使用基于静态 IP 地址的群集，将不会自动创建群集中转站。 需要手动配置群集代理。 [详细了解](http://social.technet.microsoft.com/wiki/contents/articles/18792.configure-replica-broker-role-cluster-to-cluster-replication.aspx)。 |\r | **提供程序** |在站点恢复部署期间，需要在 VMM 服务器上安装 Azure Site Recovery 提供程序。 提供程序通过 HTTPS 443 与站点恢复通信，以协调复制。 数据复制是通过 LAN 或 VPN 连接在主要和辅助 Hyper-V 服务器之间发生的。<br/><br/> 在 VMM 服务器上运行的提供程序需有权访问以下 URL：*.hypervrecoverymanager.windowsazure.cn、*.accesscontrol.chinacloudapi.cn、*.backup.windowsazure.cn、*.blob.core.chinacloudapi.cn 和 *.store.core.chinacloudapi.cn。<br/><br/> 此外，还要允许从 VMM 服务器到 [Azure 数据中心 IP 范围](https://www.microsoft.com/download/details.aspx?id=42064)的防火墙通信，并允许 HTTPS (443) 协议。 |\r \r ### <a name=\"network-mapping-prerequisites\"></a>网络映射先决条件\r 网络映射在主要和辅助 VMM 服务器上的 VMM VM 网络之间进行映射，以便执行以下操作：\r \r * 故障转移后，以最佳方式在辅助 Hyper-V 主机上放置副本 VM。\r * 将副本 VM 连接到适当的 VM 网络。\r * 如果不配置网络映射，则故障转移之后，副本 VM 将不会连接到任何网络。\r * 如果想要在站点恢复部署期间设置网络映射，则需要执行以下操作：\r \r   * 确保源 Hyper-V 主机服务器上的 VM 已连接到 VMM VM 网络。 该网络应当该链接到与该云相关联的逻辑网络。\r   * 验证用于恢复的辅助云中是否配置了相应的 VM 网络。 该 VM 网络应该链接到与辅助云关联的逻辑网络。\r \r 在以下文章中详细了解如何配置 VMM 网络\r \r * [如何在 VMM 中配置逻辑网络](http://go.microsoft.com/fwlink/p/?LinkId=386307)\r * [如何在 VMM 中配置 VM 网络和网关](http://go.microsoft.com/fwlink/p/?LinkId=386308)\r \r ### <a name=\"powershell-prerequisites\"></a>PowerShell 必决条件\r 确保已将 Azure PowerShell 准备就绪。 如果已使用 PowerShell，则升级到 0.8.10 或更高版本。 有关设置 PowerShell 的信息，请参阅 [Guide to install and configure Azure PowerShell](https://docs.microsoft.com/powershell/azureps-cmdlets-docs)（Azure PowerShell 安装和配置指南）。 安装并配置 PowerShell 后，可在[此处](https://docs.microsoft.com/powershell/azure/overview)查看该服务的所有可用 cmdlet。\r \r 若要获取有关使用 cmdlet 的帮助提示（如在 Azure PowerShell 中通常如何处理参数值、输入和输出），请参阅 [Azure Cmdlet 入门指南](https://docs.microsoft.com/powershell/azure/get-started-azureps)。\r \r ## <a name=\"step-1-set-the-subscription\"></a>步骤 1：设置订阅\r 1. 从 Azure powershell 登录到 Azure 帐户：使用以下 cmdlet\r \r         $UserName = \"<user@live.com>\"\r         $Password = \"<password>\"\r         $SecurePassword = ConvertTo-SecureString -AsPlainText $Password -Force\r         $Cred = New-Object System.Management.Automation.PSCredential -ArgumentList $UserName, $SecurePassword\r         Login-AzureRmAccount -EnvironmentName AzureChinaCloud #-Credential $Cred\r 2. 获取订阅的列表。 其中还会列出每个订阅的 subscriptionID。 记下希望在其中创建恢复服务保管库的订阅的订阅 ID    \r \r         Get-AzureRmSubscription\r 3. 通过提及订阅 ID 设置要在其中创建恢复服务保管库的订阅\r \r         Set-AzureRmContext -SubscriptionID <subscriptionId>\r \r ## <a name=\"step-2-create-a-recovery-services-vault\"></a>步骤 2：创建恢复服务保管库\r 1. 如果还没有 Azure Resource Manager 资源组，则创建一个\r \r         New-AzureRmResourceGroup -Name #ResourceGroupName -Location #location\r 2. 创建新的恢复服务保管库，并将恢复服务保管库对象保存在变量（后面将用到）中。 也可以在创建后使用 Get-AzureRMRecoveryServicesVault cmdlet 检索保管库对象：-\r \r         $vault = New-AzureRmRecoveryServicesVault -Name #vaultname -ResouceGroupName #ResourceGroupName -Location #location\r \r ## <a name=\"step-3-set-the-recovery-services-vault-context\"></a>步骤 3：设置恢复服务保管库上下文\r 1. 如果已创建保管库，则运行以下命令来获取该保管库。\r \r         $vault = Get-AzureRmRecoveryServicesVault -Name #vaultname\r 2. 通过运行以下命令设置保管库上下文。\r \r         Set-AzureRmSiteRecoveryVaultSettings -ARSVault $vault\r \r ## <a name=\"step-4-install-the-azure-site-recovery-provider\"></a>步骤 4：安装 Azure Site Recovery 提供者\r 1. 在 VMM 计算机上，通过运行以下命令创建一个目录：\r \r         New-Item c:\\ASR -type directory\r 2. 通过运行以下命令，使用下载的提供程序提取文件\r \r         pushd C:\\ASR\\\r         .\\AzureSiteRecoveryProvider.exe /x:. /q\r 3. 使用以下命令安装提供程序：\r \r         .\\SetupDr.exe /i\r         $installationRegPath = \"hklm:\\software\\Microsoft\\Microsoft System Center Virtual Machine Manager Server\\DRAdapter\"\r         do\r         {\r          $isNotInstalled = $true;\r          if(Test-Path $installationRegPath)\r          {\r            $isNotInstalled = $false;\r          }\r         }While($isNotInstalled)\r \r     等待安装完成。\r 4. 使用以下命令在保管库中注册服务器：\r \r         $BinPath = $env:SystemDrive+\"\\Program Files\\Microsoft System Center 2012 R2\\Virtual Machine Manager\\bin\"\r         pushd $BinPath\r         $encryptionFilePath = \"C:\\temp\\\".\\DRConfigurator.exe /r /Credentials $VaultSettingFilePath /vmmfriendlyname $env:COMPUTERNAME /dataencryptionenabled $encryptionFilePath /startvmmservice\r \r ## <a name=\"step-5-create-and-associate-a-replication-policy\"></a>步骤 5：创建和关联复制策略\r 1. 通过运行以下命令创建 Hyper-V 2012 R2 复制策略：\r \r         $ReplicationFrequencyInSeconds = \"300\";        #options are 30,300,900\r         $PolicyName = \"replicapolicy\"\r         $RepProvider = HyperVReplica2012R2\r         $Recoverypoints = 24                    #specify the number of hours to retain recovery pints\r         $AppConsistentSnapshotFrequency = 4 #specify the frequency (in hours) at which app consistent snapshots are taken\r         $AuthMode = \"Kerberos\"  #options are \"Kerberos\" or \"Certificate\"\r         $AuthPort = \"8083\"  #specify the port number that will be used for replication traffic on Hyper-V hosts\r         $InitialRepMethod = \"Online\" #options are \"Online\" or \"Offline\"\r \r         $policyresult = New-AzureRmSiteRecoveryPolicy -Name $policyname -ReplicationProvider $RepProvider -ReplicationFrequencyInSeconds $Replicationfrequencyinseconds -RecoveryPoints $recoverypoints -ApplicationConsistentSnapshotFrequencyInHours $AppConsistentSnapshotFrequency -Authentication $AuthMode -ReplicationPort $AuthPort -ReplicationMethod $InitialRepMethod\r \r     > [!NOTE]\r     > VMM 云包含的 Hyper-V 主机可能运行不同版本的 Windows Server（如 Hyper-V 先决条件中所示），但复制策略是特定于 OS 版本的。 如果不同的主机运行在不同的操作系统版本上，则请为每类 OS 版本创建不同的复制策略。 例如，如果有 5 个主机运行在 Windows Server 2012 上，3 个主机运行在 Windows Server 2012 R2 上，请创建两个复制策略 - 一个策略用于一种类型的操作系统版本。\r \r 2. 通过运行以下命令获取主保护容器（主 VMM 云）和恢复保护容器（恢复 VMM 云）：\r \r         $PrimaryCloud = \"testprimarycloud\"\r         $primaryprotectionContainer = Get-AzureRmSiteRecoveryProtectionContainer -friendlyName $PrimaryCloud;  \r \r         $RecoveryCloud = \"testrecoverycloud\"\r         $recoveryprotectionContainer = Get-AzureRmSiteRecoveryProtectionContainer -friendlyName $RecoveryCloud;  \r 2. 使用该策略的友好名称检索在步骤 1 中创建的策略\r \r         $policy = Get-AzureRmSiteRecoveryPolicy -FriendlyName $policyname\r 3. 开始将保护容器（VMM 云）与复制策略相关联：\r \r         $associationJob  = Start-AzureRmSiteRecoveryPolicyAssociationJob -Policy     $Policy -PrimaryProtectionContainer $primaryprotectionContainer -RecoveryProtectionContainer $recoveryprotectionContainer\r 4. 等待策略关联作业完成。 可以使用以下 PowerShell 代码段来查看作业是否已完成。\r \r         $job = Get-AzureRmSiteRecoveryJob -Job $associationJob\r \r         if($job -eq $null -or $job.StateDescription -ne \"Completed\")\r         {\r          $isJobLeftForProcessing = $true;\r         }\r \r     在作业完成处理后，运行以下命令：\r \r         if($isJobLeftForProcessing)\r         {\r         Start-Sleep -Seconds 60\r         }\r         }While($isJobLeftForProcessing)\r \r 若要检查作业是否完成，请遵循 [监视活动](#monitor)中的步骤。\r \r ## <a name=\"step-6-configure-network-mapping\"></a>步骤 6：配置网络映射\r 1. 第一条命令获取当前 Azure Site Recovery 保管库的服务器。 该命令将 Azure Site Recovery 服务器存储在 $Servers 数组变量中。\r \r         $Servers = Get-AzureRmSiteRecoveryServer\r 2. 以下命令获取源 VMM 服务器和目标 VMM 服务器的 Site Recovery 网络。\r \r         $PrimaryNetworks = Get-AzureRmSiteRecoveryNetwork -Server $Servers[0]        \r \r         $RecoveryNetworks = Get-AzureRmSiteRecoveryNetwork -Server $Servers[1]\r \r     > [!NOTE]\r     > 在服务器数组中，源 VMM 服务器可以是一个，也可以是第二个。 查看 VMM 服务器的名称，获取相应的网络\r \r 3. 最后一个 cmdlet 会在主网络与恢复网络之间创建映射。 该 cmdlet 将主要网络指定为 $PrimaryNetworks 的第一个元素，将恢复网络指定为 $RecoveryNetworks 的第一个元素。\r \r         New-AzureRmSiteRecoveryNetworkMapping -PrimaryNetwork $PrimaryNetworks[0] -RecoveryNetwork $RecoveryNetworks[0]\r \r ## <a name=\"step-7-configure-storage-mapping\"></a>步骤 7：配置存储映射\r 1. 以下命令在 $storageclassifications 变量中获取存储分类的列表。\r \r         $storageclassifications = Get-AzureRmSiteRecoveryStorageClassification\r 2. 以下命令在 $SourceClassificaion 变量中获取源分类，在 $TargetClassification 变量中获取目标分类。\r \r         $SourceClassificaion = $storageclassifications[0]\r \r         $TargetClassification = $storageclassifications[1]\r \r     > [!NOTE]\r     > 源和目标分类可以是数组中的任何元素。 请参考以下命令的输出，了解 $storageclassifications 数组中源和目标分类的索引。 \r \r     > Get-AzureRmSiteRecoveryStorageClassification | Select-Object -Property FriendlyName, Id | Format-Table\r \r 1. 以下 cmdlet 在源分类与目标分类之间创建映射。 \r \r         New-AzureRmSiteRecoveryStorageClassificationMapping -PrimaryStorageClassification $SourceClassificaion -RecoveryStorageClassification $TargetClassification\r \r ## <a name=\"step-8-enable-protection-for-virtual-machines\"></a>步骤 8：为虚拟机启用保护\r 在正确配置服务器、云和网络后，可以在云中为虚拟机启用保护。 \r \r 1. 若要启用保护，请运行以下命令以获取保护容器：\r \r         $PrimaryProtectionContainer = Get-AzureRmSiteRecoveryProtectionContainer -friendlyName $PrimaryCloudName\r 2. 通过运行以下命令获取保护实体 (VM)：\r \r         $protectionEntity = Get-AzureRmSiteRecoveryProtectionEntity -friendlyName $VMName -ProtectionContainer $PrimaryProtectionContainer\r 3. 通过运行以下命令为 VM 启用复制：\r \r         $jobResult = Set-AzureRmSiteRecoveryProtectionEntity -ProtectionEntity $protectionentity -Protection Enable -Policy $policy\r \r ## <a name=\"test-your-deployment\"></a>测试部署\r 若要测试部署，可针对一台虚拟机运行测试故障转移，或者创建一个包括多个虚拟机的恢复计划并针对该计划运行测试故障转移。 测试故障转移在隔离的网络中模拟用户的故障转移和恢复机制。\r \r > [!NOTE]\r > 可以在 Azure 门户中为应用程序创建恢复计划。\r >\r >\r \r 若要检查作业是否完成，请遵循 [监视活动](#monitor)中的步骤。\r \r ### <a name=\"run-a-test-failover\"></a>运行测试故障转移\r 1. 运行以下 cmdlet 所获取的 VM 网络是需要将 VM 通过测试性故障转移方式转移到其中的网络。\r \r         $Servers = Get-AzureRmSiteRecoveryServer\r         $RecoveryNetworks = Get-AzureRmSiteRecoveryNetwork -Server $Servers[1]\r 2. 执行以下操作，对 VM 进行测试性故障转移：\r \r         $protectionEntity = Get-AzureRmSiteRecoveryProtectionEntity -FriendlyName $VMName -ProtectionContainer $PrimaryprotectionContainer\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryTestFailoverJob -Direction PrimaryToRecovery -ProtectionEntity $protectionEntity -VMNetwork $RecoveryNetworks[1]\r 3. 执行以下操作，对恢复计划进行测试性故障转移：\r \r         $recoveryplanname = \"test-recovery-plan\"\r \r         $recoveryplan = Get-AzureRmSiteRecoveryRecoveryPlan -FriendlyName $recoveryplanname\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryTestFailoverJob -Direction PrimaryToRecovery -Recoveryplan $recoveryplan -VMNetwork $RecoveryNetworks[1]\r \r ### <a name=\"run-a-planned-failover\"></a>运行计划的故障转移\r 1. 执行以下操作，对 VM 进行计划内故障转移：\r \r         $protectionEntity = Get-AzureRmSiteRecoveryProtectionEntity -Name $VMName -ProtectionContainer $PrimaryprotectionContainer\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryPlannedFailoverJob -Direction PrimaryToRecovery -ProtectionEntity $protectionEntity\r 2. 执行以下操作，对恢复计划进行计划内故障转移：\r \r         $recoveryplanname = \"test-recovery-plan\"\r \r         $recoveryplan = Get-AzureRmSiteRecoveryRecoveryPlan -FriendlyName $recoveryplanname\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryPlannedFailoverJob -Direction PrimaryToRecovery -Recoveryplan $recoveryplan\r \r ### <a name=\"run-an-unplanned-failover\"></a>运行非计划的故障转移\r 1. 执行以下操作，对 VM 进行计划外故障转移：\r \r         $protectionEntity = Get-AzureRmSiteRecoveryProtectionEntity -Name $VMName -ProtectionContainer $PrimaryprotectionContainer\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryUnPlannedFailoverJob -Direction PrimaryToRecovery -ProtectionEntity $protectionEntity\r \r 2. 执行以下操作，对恢复计划进行计划外故障转移：\r \r         $recoveryplanname = \"test-recovery-plan\"\r \r         $recoveryplan = Get-AzureRmSiteRecoveryRecoveryPlan -FriendlyName $recoveryplanname\r \r         $jobIDResult =  Start-AzureRmSiteRecoveryUnPlannedFailoverJob -Direction PrimaryToRecovery -ProtectionEntity $protectionEntity\r \r ## <a name=monitor></a> 监视活动\r 使用以下命令来监视活动。 请注意，必须在执行不同的作业之前等待处理完成。\r \r     Do\r     {\r         $job = Get-AzureSiteRecoveryJob -Id $associationJob.JobId;\r         Write-Host \"Job State:{0}, StateDescription:{1}\" -f Job.State, $job.StateDescription;\r         if($job -eq $null -or $job.StateDescription -ne \"Completed\")\r         {\r             $isJobLeftForProcessing = $true;\r         }\r \r         if($isJobLeftForProcessing)\r         {\r             Start-Sleep -Seconds 60\r         }\r     }While($isJobLeftForProcessing)\r \r ## <a name=\"next-steps\"></a>后续步骤\r [详细了解](https://docs.microsoft.com/powershell/module/azurerm.recoveryservices.backup/#recovery) Azure Site Recovery 和 Azure Resource Manager PowerShell cmdlet。\r \r <!-- Update_Description: update meta properties, wording update -->"}