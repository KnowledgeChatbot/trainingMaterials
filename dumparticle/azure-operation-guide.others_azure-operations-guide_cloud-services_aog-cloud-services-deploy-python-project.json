{"Title":"如何在云服务中使用离线包发布 Python 应用程序","Description":"如何在云服务中使用离线包发布 Python 应用程序","Content":"# 如何在云服务中使用离线包发布 Python 应用程序\r \r 当用户使用 Azure 云服务发布 Python Work Role 应用程序时，在启动应用程序之前，需要先下载并安装 Python 环境，并且使用 pip 安装必需的第三方包。但这个过程中，往往会受到网络的影响导致下载失败，从而致使云服务应用启动失败。本文将介绍一种使用离线 Python 下载包安装 Python 运行环境，并缓存 pip 离线包来加速 Python 应用程序的启动的方法。\r \r 首先，我们需要按照官方推荐的方法来创建 Python Work Role 项目，具体可参考此文档：[用于 Visual Studio 的 Python 工具中的 Python Web 角色和辅助角色](https://docs.azure.cn/zh-cn/cloud-services/cloud-services-python-ptvs)。\r \r 之后，我们需要从 Python 官方网站下载离线安装包，并将其放置到项目脚本目录下。\r \r ![01](media/aog-cloud-services-deploy-python-project/01.png)\r \r 然后，创建用来存放离线第三方包的 cache 目录，下载第三方依赖包，并放置到该目录下，Python 包的依赖可能比较复杂，我们可以执行以下命令下载依赖包，使用离线依赖的作用就是可以防止在云服务上再去下载依赖，减少云服务启动时间。\r \r `py -m pip download -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/ -d cache --timeout 60`\r \r 最后，修改 Python 脚本，使其能够使用本地包，来安装 Python 运行环境，参考以下完整脚本 :\r \r ```Python\r $is_emulated = $env:EMULATED -eq \"true\"\r $is_python2 = $env:PYTHON2 -eq \"on\"\r $nl = [Environment]::NewLine\r \r if (-not $is_emulated){\r     Write-Output \"Checking if python is installed...$nl\"\r     if ($is_python2) {\r         & \"${env:SystemDrive}\\Python27\\python.exe\"  -V | Out-Null\r     }\r     else {\r         py -V | Out-Null\r     }\r \r     if (-not $?) {\r \r         $outFile = \"$(Get-Location)\\python-3.5.2-amd64.exe\"\r \r         if ($is_python2) {\r             $url = \"https://www.python.org/ftp/python/2.7.12/python-2.7.12.amd64.msi\"\r             $outFile = \"${env:TEMP}\\python-2.7.12.amd64.msi\"\r         }\r \r         Write-Output \"Install $outFile$nl\"\r         \r \r         if ($is_python2) {\r \t\t\tWrite-Output \"Installing Python2$nl\"\r             Start-Process msiexec.exe -ArgumentList \"/q\", \"/i\", \"$outFile\", \"ALLUSERS=1\" -Wait\r         }\r         else {\r \t\t\tWrite-Output \"Installing Python3$nl\"\r             Start-Process \"$outFile\" -ArgumentList \"/quiet\", \"InstallAllUsers=1\" -Wait\r         }\r \r         Write-Output \"Done$nl\"\r     }\r     else {\r         Write-Output \"Already installed\"\r     }\r }\r \r if (-not $is_emulated){\r     Write-Output \"Checking if requirements.txt exists$nl\"\r     if (Test-Path ..\\requirements.txt) {\r         Write-Output \"Found. Processing pip$nl\"\r \r         if ($is_python2) {\r \t\t\tcd..\r             & \"${env:SystemDrive}\\Python27\\python.exe\" -m pip download -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/ -d cache  --timeout 60\r \t\t\t& \"${env:SystemDrive}\\Python27\\python.exe\" -m pip install -r requirements.txt --find-links cache\r \r         }\r         else {\r \t\t\tcd..\r \t\t\tWrite-Output \"Downloading Package$nl\"\r             py -m pip download -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/ -d cache --timeout 60\r \t\t\tWrite-Output \"Installing Package$nl\"\r \t\t\tpy -m pip install -r requirements.txt --find-links cache\r         }\r         Write-Output \"Done$nl\"\r     }\r     else {\r         Write-Output \"Not found$nl\"\r     }\r }\r ```\r \r 通过以上操作，我们就可以使用离线包，在 Cloud Service 上安装 Python 运行环境，大大降低了出现问题的几率和发布的时间。\r "}