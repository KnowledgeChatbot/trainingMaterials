{"Title":"将 Python UDF 与 Apache Hive 和 Pig 配合使用 - Azure HDInsight","Description":"了解如何在 HDInsight（Azure 上的 Hadoop 技术堆栈）中通过 Hive 和 Pig 使用 Python 用户定义的函数 (UDF)。","Content":"# <a name=\"use-python-user-defined-functions-udf-with-hive-and-pig-in-hdinsight\"></a>在 HDInsight 中通过 Hive 和 Pig 使用 Python 用户定义的函数 (UDF)\r \r [!INCLUDE [hdinsight-linux-acn-version.md](../../../includes/hdinsight-linux-acn-version.md)]\r \r 了解如何在 Azure HDInsight 上将 Python 用户定义的函数 (UDF) 与 Apache Hive 和 Pig 配合使用。\r \r ## <a name=\"python\"></a>HDInsight 上的 Python\r \r 默认情况下，Python2.7 安装在 HDInsight 3.0 和更高版本上。 可将 Apache Hive 与此版 Python 配合使用以进行流式处理。 流式处理使用 STDOUT 和 STDIN 在 Hive 与 UDF 之间传递数据。\r \r HDInsight 还包含 Jython，后者是用 Java 编写的 Python 实现。 Jython 在 Java 虚拟机上直接运行，不使用流式处理。 将 Python 与 Pig 配合使用时，建议使用 Jython 作为 Python 解释器。\r \r > [!WARNING]\r > 本文档中的步骤基于以下假设： \r >\r > * 在本地开发环境中创建 Python 脚本。\r > * 通过本地 Bash 会话使用 `scp` 命令或使用提供的 PowerShell 脚本将脚本上传到 HDInsight。\r \r ## <a name=\"hivepython\"></a>Hive UDF\r \r 可以通过 HiveQL `TRANSFORM` 语句将 Python 用作 Hive 中的 UDF。 例如，以下 HiveQL 调用群集默认 Azure 存储帐户中存储的 `hiveudf.py` 文件。\r \r **基于 Linux 的 HDInsight**\r \r ```hiveql\r add file wasb:///hiveudf.py;\r \r SELECT TRANSFORM (clientid, devicemake, devicemodel)\r     USING 'python hiveudf.py' AS\r     (clientid string, phoneLable string, phoneHash string)\r FROM hivesampletable\r ORDER BY clientid LIMIT 50;\r ```\r \r **基于 Windows 的 HDInsight**\r \r ```hiveql\r add file wasb:///hiveudf.py;\r \r SELECT TRANSFORM (clientid, devicemake, devicemodel)\r     USING 'D:\\Python27\\python.exe hiveudf.py' AS\r     (clientid string, phoneLable string, phoneHash string)\r FROM hivesampletable\r ORDER BY clientid LIMIT 50;\r ```\r \r > [!NOTE]\r > 在基于 Windows 的 HDInsight 群集上，`USING` 子句必须指定 python.exe 的完整路径。\r \r 下面是本示例执行的操作：\r \r 1. 文件开头的 `add file` 语句将 `hiveudf.py` 文件添加到分布式缓存，使群集中的所有节点都可访问该文件。\r 2. `SELECT TRANSFORM ... USING` 语句从 `hivesampletable` 中选择数据。 它还将 clientid、devicemake 和 devicemodel 值传递到 `hiveudf.py` 脚本。\r 3. `AS` 子句描述从 `hiveudf.py` 返回的字段。\r \r <a name=\"streamingpy\"></a>\r \r ### <a name=\"create-the-hiveudfpy-file\"></a>创建 hiveudf.py 文件\r \r 在开发环境中，创建名为 `hiveudf.py` 的文本文件。 将以下代码用作该文件的内容：\r \r ```python\r #!/usr/bin/env python\r import sys\r import string\r import hashlib\r \r while True:\r     line = sys.stdin.readline()\r     if not line:\r         break\r \r     line = string.strip(line, \"\\n \")\r     clientid, devicemake, devicemodel = string.split(line, \"\\t\")\r     phone_label = devicemake + ' ' + devicemodel\r     print \"\\t\".join([clientid, phone_label, hashlib.md5(phone_label).hexdigest()])\r ```\r \r 此脚本可执行以下操作：\r \r 1. 从 STDIN 读取一行数据。\r 2. 尾随的换行符使用 `string.strip(line, \"\\n \")`删除。\r 3. 执行流式处理时，一个行就包含了所有值，每两个值之间有一个制表符。 因此， `string.split(line, \"\\t\")` 可用于在每个制表符处拆分输入，并只返回字段。\r 4. 在处理完成后，必须将输出以单行形式写入到 STDOUT，并在每两个字段之间提供一个制表符。 例如， `print \"\\t\".join([clientid, phone_label, hashlib.md5(phone_label).hexdigest()])`。\r 5. `while` 循环一直重复到没有 `line` 可以读取。\r \r 脚本输出是 `devicemake` 和 `devicemodel` 的输入值的连接，并且是连接值的哈希。\r \r 有关如何在 HDInsight 群集上运行此示例的信息，请参阅[运行示例](#running)。\r \r ## <a name=\"pigpython\"></a>Pig UDF\r \r 在整个 `GENERATE` 语句中，Python 脚本可用作 Pig 中的 UDF。 可以使用 Jython 或 C Python 运行此脚本。\r \r * Jython 在 JVM 上运行，并且原本就能从 Pig 调用。\r * C Python 是外部进程，因此，JVM 上的 Pig 中的数据将发送到 Python 进程中运行的脚本。 Python 脚本的输出将发回到 Pig 中。\r \r 若要指定 Python 解释器，请在引用 Python 脚本时使用 `register`。 以下示例将脚本作为 `myfuncs` 注册到 Pig：\r \r * **使用 Jython**：`register '/path/to/pigudf.py' using jython as myfuncs;`\r * **使用 C Python**：`register '/path/to/pigudf.py' using streaming_python as myfuncs;`\r \r > [!IMPORTANT]\r > 使用 Jython 时，pig_jython 文件的路径可以是本地路径或 WASB:// 路径。 但是，使用 C Python 时，必须引用用于提交 Pig 作业的节点的本地文件系统上的文件。\r \r 通过注册后，此示例的 Pig Latin 对于两个脚本是相同的：\r \r ```pig\r LOGS = LOAD 'wasb:///example/data/sample.log' as (LINE:chararray);\r LOG = FILTER LOGS by LINE is not null;\r DETAILS = FOREACH LOG GENERATE myfuncs.create_structure(LINE);\r DUMP DETAILS;\r ```\r \r 下面是本示例执行的操作：\r \r 1. 第一行代码将示例数据文件 `sample.log` 加载到 `LOGS` 中。 它还将每个记录定义为 `chararray`。\r 2. 第二行代码筛选出所有 null 值，并将操作结果存储在 `LOG` 中。\r 3. 接下来，它将循环访问 `LOG` 中的记录，并使用 `GENERATE` 来调用作为 `myfuncs` 加载的 Python/Jython 脚本中包含的 `create_structure` 方法。 `LINE` 用于将当前记录传递给函数。\r 4. 最后，使用 `DUMP` 命令将输出转储到 STDOUT。 操作完成后，此命令会显示结果。\r \r ### <a name=\"create-the-pigudfpy-file\"></a>创建 pigudf.py 文件\r \r 在开发环境中，创建名为 `pigudf.py` 的文本文件。 将以下代码用作该文件的内容：\r \r <a name=\"streamingpy\"></a>\r \r ```python\r # Uncomment the following if using C Python\r #from pig_util import outputSchema\r \r @outputSchema(\"log: {(date:chararray, time:chararray, classname:chararray, level:chararray, detail:chararray)}\")\r def create_structure(input):\r     if (input.startswith('java.lang.Exception')):\r         input = input[21:len(input)] + ' - java.lang.Exception'\r     date, time, classname, level, detail = input.split(' ', 4)\r     return date, time, classname, level, detail\r ```\r \r 在 Pig Latin 示例中，我们已将 `LINE` 输入定义为 chararray，因为输入没有一致的架构。 Python 脚本将数据转换成用于输出的一致架构。\r \r 1. `@outputSchema` 语句定义返回到 Pig 的数据的格式。 在本例中，该格式为**数据袋**，这是一种 Pig 数据类型。 该数据袋包含以下字段，所有这些字段都是 chararray（字符串）：\r \r    * date - 创建日志条目的日期\r    * time - 创建日志条目的时间\r    * classname - 为其创建该条目的类名\r    * level - 日志级别\r    * detail - 日志条目的详细信息\r \r 2. 接下来，`def create_structure(input)` 定义一个函数，以便 Pig 将行项传递到其中。\r \r 3. 示例数据 `sample.log` 基本上符合我们要返回的日期、时间、类名、级别和详细信息架构。 但是，它还包含了一些以字符串 `*java.lang.Exception*` 开头的行。 我们需要修改这些行，使之与架构匹配。 `if` 语句将检查这些行，然后调整输入数据以将 `*java.lang.Exception*` 字符串移到末尾，使数据与预期的输出架构相一致。\r \r 4. 接下来，使用 `split` 命令在前四个空格字符处拆分数据。 输出将分配到 `date`、`time`、`classname`、`level` 和 `detail`。\r \r 5. 最后，将值返回到 Pig。\r \r 数据返回到 Pig 时，其架构与 `@outputSchema` 语句中的定义一致。\r \r ## <a name=\"running\"></a>上传并运行示例\r \r > [!IMPORTANT]\r > **SSH** 步骤仅适用于基于 Linux 的 HDInsight 群集。 **PowerShell** 步骤适用于基于 Linux 或 Windows 的 HDInsight 群集，但需要 Windows 客户端。\r \r ### <a name=\"ssh\"></a>SSH\r \r 有关使用 SSH 的详细信息，请参阅[将 SSH 与 HDInsight 配合使用](../hdinsight-hadoop-linux-use-ssh-unix.md)。\r \r 1. 使用 `scp` 将文件复制到 HDInsight 群集。 例如，以下命令会将文件复制到名为 **mycluster** 的群集。\r \r     ```bash\r     scp hiveudf.py pigudf.py myuser@mycluster-ssh.azurehdinsight.cn:\r     ```\r \r 2. 使用 SSH 连接到群集。\r \r     ```bash\r     ssh myuser@mycluster-ssh.azurehdinsight.cn\r     ```\r \r     有关详细信息，请参阅[将 SSH 与 HDInsight 配合使用](../hdinsight-hadoop-linux-use-ssh-unix.md)文档。\r \r 3. 从 SSH 会话将前面上传的 python 文件添加到群集的 WASB 存储中。\r \r     ```bash\r     hdfs dfs -put hiveudf.py /hiveudf.py\r     hdfs dfs -put pigudf.py /pigudf.py\r     ```\r \r 在上传文件后，使用以下步骤来运行 Hive 和 Pig 作业。\r \r #### <a name=\"use-the-hive-udf\"></a>使用 Hive UDF\r \r 1. 要连接到 Hive，请使用以下命令：\r \r     ```bash\r     beeline -u 'jdbc:hive2://headnodehost:10001/;transportMode=http'\r     ```\r \r     此命令启动 Beeline 客户端。\r \r 2. 在 `0: jdbc:hive2://headnodehost:10001/>` 提示符下输入以下查询：\r \r    ```hive\r    add file wasb:///hiveudf.py;\r    SELECT TRANSFORM (clientid, devicemake, devicemodel)\r        USING 'python hiveudf.py' AS\r        (clientid string, phoneLabel string, phoneHash string)\r    FROM hivesampletable\r    ORDER BY clientid LIMIT 50;\r    ```\r \r 3. 在输入最后一行后，该作业应该启动。 在作业完成后，会返回类似于以下示例的输出：\r \r         100041    RIM 9650    d476f3687700442549a83fac4560c51c\r         100041    RIM 9650    d476f3687700442549a83fac4560c51c\r         100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r         100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r         100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r \r 4. 要退出 Beeline，请使用以下命令：\r \r     ```hive\r     !q\r     ```\r \r #### <a name=\"use-the-pig-udf\"></a>使用 Pig UDF\r \r 1. 要连接到 Pig，请使用以下命令：\r \r     ```bash\r     pig\r     ```\r \r 2. 在 `grunt>` 提示符下输入以下语句：\r \r    ```pig\r    Register wasb:///pigudf.py using jython as myfuncs;\r    LOGS = LOAD 'wasb:///example/data/sample.log' as (LINE:chararray);\r    LOG = FILTER LOGS by LINE is not null;\r    DETAILS = foreach LOG generate myfuncs.create_structure(LINE);\r    DUMP DETAILS;\r    ```\r \r 3. 输入以下行后，应会启动作业。 作业完成后，将返回类似于以下数据的输出：\r \r         ((2012-02-03,20:11:56,SampleClass5,[TRACE],verbose detail for id 990982084))\r         ((2012-02-03,20:11:56,SampleClass7,[TRACE],verbose detail for id 1560323914))\r         ((2012-02-03,20:11:56,SampleClass8,[DEBUG],detail for id 2083681507))\r         ((2012-02-03,20:11:56,SampleClass3,[TRACE],verbose detail for id 1718828806))\r         ((2012-02-03,20:11:56,SampleClass3,[INFO],everything normal for id 530537821))\r \r 4. 使用 `quit` 退出 Grunt shell，并在本地文件系统上使用以下命令编辑 pigudf.py 文件：\r \r     ```bash\r     nano pigudf.py\r     ```\r \r 5. 进入编辑器后，删除行开头的 `#` 字符以取消注释以下行：\r \r     ```bash\r     #from pig_util import outputSchema\r     ```\r \r     这将修改 Python 脚本以使用 C Python 而不是 Jython。 更改后，请使用 Ctrl+X 退出编辑器。 选择 Y，然后选择 Enter 保存更改。\r \r 6. 使用 `pig` 命令再次启动 shell。 在 `grunt>` 提示符下，使用以下命令运行带有 Jython 解释器的 Python 脚本。\r \r    ```pig\r    Register 'pigudf.py' using streaming_python as myfuncs;\r    LOGS = LOAD 'wasb:///example/data/sample.log' as (LINE:chararray);\r    LOG = FILTER LOGS by LINE is not null;\r    DETAILS = foreach LOG generate myfuncs.create_structure(LINE);\r    DUMP DETAILS;\r    ```\r \r     完成此作业后，看到的输出应该与之前使用 Jython 运行脚本后的输出相同。\r \r ### <a name=\"powershell-upload-the-files\"></a>PowerShell：上传文件\r \r 可以使用 PowerShell 将文件上传到 HDInsight 服务器。 使用以下脚本上传 Python 文件：\r \r > [!IMPORTANT] \r > 本部分中的步骤使用 Azure PowerShell。 有关如何使用 Azure PowerShell 的详细信息，请参阅[如何安装和配置 Azure PowerShell](https://docs.microsoft.com/powershell/azure/overview)。\r \r ```powershell\r # Login to your Azure subscription\r # Is there an active Azure subscription?\r $sub = Get-AzureRmSubscription -ErrorAction SilentlyContinue\r if(-not($sub))\r {\r     Add-AzureRmAccount -EnvironmentName AzureChinaCloud\r }\r \r # Get cluster info\r $clusterName = Read-Host -Prompt \"Enter the HDInsight cluster name\"\r # Change the path to match the file location on your system\r $pathToStreamingFile = \"C:\\path\\to\\hiveudf.py\"\r $pathToJythonFile = \"C:\\path\\to\\pigudf.py\"\r \r $clusterInfo = Get-AzureRmHDInsightCluster -ClusterName $clusterName\r $resourceGroup = $clusterInfo.ResourceGroup\r $storageAccountName=$clusterInfo.DefaultStorageAccount.split('.')[0]\r $container=$clusterInfo.DefaultStorageContainer\r $storageAccountKey=(Get-AzureRmStorageAccountKey `\r     -Name $storageAccountName `\r -ResourceGroupName $resourceGroup)[0].Value\r \r #Create a storage content and upload the file\r $context = New-AzureStorageContext `\r     -StorageAccountName $storageAccountName `\r     -StorageAccountKey $storageAccountKey\r \r Set-AzureStorageBlobContent `\r     -File $pathToStreamingFile `\r     -Blob \"hiveudf.py\" `\r     -Container $container `\r     -Context $context\r \r Set-AzureStorageBlobContent `\r     -File $pathToJythonFile `\r     -Blob \"pigudf.py\" `\r     -Container $container `\r     -Context $context\r ```\r > [!IMPORTANT]\r > 将 `C:\\path\\to` 值更改为开发环境中的文件路径。\r \r 此脚本将检索 HDInsight 群集的信息，并提取默认存储帐户的名称和密钥，并将文件上传到容器的根目录。\r \r > [!NOTE]\r > 有关上传文件的详细信息，请参阅[在 HDInsight 中上传 Hadoop 作业的数据](../hdinsight-upload-data.md)文档。\r \r #### <a name=\"powershell-use-the-hive-udf\"></a>PowerShell：使用 Hive UDF\r \r 也可以使用 PowerShell 远程运行 Hive 查询。 使用以下 PowerShell 脚本来运行使用 **hiveudf.py** 脚本的 Hive 查询：\r \r > [!IMPORTANT]\r > 在运行之前，该脚本会提示输入 HDInsight 群集的 HTTPs/管理员帐户信息。\r \r ```powershell\r # Login to your Azure subscription\r # Is there an active Azure subscription?\r $sub = Get-AzureRmSubscription -ErrorAction SilentlyContinue\r if(-not($sub))\r {\r     Add-AzureRmAccount -EnvironmentName AzureChinaCloud\r }\r \r # Get cluster info\r $clusterName = Read-Host -Prompt \"Enter the HDInsight cluster name\"\r $creds=Get-Credential -Message \"Enter the login for the cluster\"\r \r # If using a Windows-based HDInsight cluster, change the USING statement to:\r # \"USING 'D:\\Python27\\python.exe hiveudf.py' AS \" +\r $HiveQuery = \"add file wasb:///hiveudf.py;\" +\r                 \"SELECT TRANSFORM (clientid, devicemake, devicemodel) \" +\r                 \"USING 'python hiveudf.py' AS \" +\r                 \"(clientid string, phoneLabel string, phoneHash string) \" +\r                 \"FROM hivesampletable \" +\r                 \"ORDER BY clientid LIMIT 50;\"\r \r $jobDefinition = New-AzureRmHDInsightHiveJobDefinition `\r     -Query $HiveQuery\r \r $job = Start-AzureRmHDInsightJob `\r     -ClusterName $clusterName `\r     -JobDefinition $jobDefinition `\r     -HttpCredential $creds\r Write-Host \"Wait for the Hive job to complete ...\" -ForegroundColor Green\r Wait-AzureRmHDInsightJob `\r     -JobId $job.JobId `\r     -ClusterName $clusterName `\r     -HttpCredential $creds\r # Uncomment the following to see stderr output\r # Get-AzureRmHDInsightJobOutput `\r #   -Clustername $clusterName `\r #   -JobId $job.JobId `\r #   -HttpCredential $creds `\r #   -DisplayOutputType StandardError\r Write-Host \"Display the standard output ...\" -ForegroundColor Green\r Get-AzureRmHDInsightJobOutput `\r     -Clustername $clusterName `\r     -JobId $job.JobId `\r     -HttpCredential $creds\r ```\r \r **Hive** 作业的输出应该如以下示例所示：\r \r     100041    RIM 9650    d476f3687700442549a83fac4560c51c\r     100041    RIM 9650    d476f3687700442549a83fac4560c51c\r     100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r     100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r     100042    Apple iPhone 4.2.x    375ad9a0ddc4351536804f1d5d0ea9b9\r \r #### <a name=\"pig-jython\"></a>Pig (Jython)\r \r 也可以使用 PowerShell 运行 Pig Latin 作业。 若要运行使用 **pigudf.py** 脚本的 Pig Latin 作业，请使用以下 PowerShell 脚本：\r \r > [!NOTE]\r > 使用 PowerShell 远程提交作业时，无法使用 C Python 作为解释器。\r \r ```powershell\r # Login to your Azure subscription\r # Is there an active Azure subscription?\r $sub = Get-AzureRmSubscription -ErrorAction SilentlyContinue\r if(-not($sub))\r {\r     Add-AzureRmAccount -EnvironmentName AzureChinaCloud\r }\r \r # Get cluster info\r $clusterName = Read-Host -Prompt \"Enter the HDInsight cluster name\"\r $creds=Get-Credential -Message \"Enter the login for the cluster\"\r \r $PigQuery = \"Register wasb:///pigudf.py using jython as myfuncs;\" +\r             \"LOGS = LOAD 'wasb:///example/data/sample.log' as (LINE:chararray);\" +\r             \"LOG = FILTER LOGS by LINE is not null;\" +\r             \"DETAILS = foreach LOG generate myfuncs.create_structure(LINE);\" +\r             \"DUMP DETAILS;\"\r \r $jobDefinition = New-AzureRmHDInsightPigJobDefinition -Query $PigQuery\r \r $job = Start-AzureRmHDInsightJob `\r     -ClusterName $clusterName `\r     -JobDefinition $jobDefinition `\r     -HttpCredential $creds\r \r Write-Host \"Wait for the Pig job to complete ...\" -ForegroundColor Green\r Wait-AzureRmHDInsightJob `\r     -Job $job.JobId `\r     -ClusterName $clusterName `\r     -HttpCredential $creds\r # Uncomment the following to see stderr output\r # Get-AzureRmHDInsightJobOutput `\r #    -Clustername $clusterName `\r #    -JobId $job.JobId `\r #    -HttpCredential $creds `\r #    -DisplayOutputType StandardError\r Write-Host \"Display the standard output ...\" -ForegroundColor Green\r Get-AzureRmHDInsightJobOutput `\r     -Clustername $clusterName `\r     -JobId $job.JobId `\r     -HttpCredential $creds\r ```\r \r **Pig** 作业的输出应类似于以下数据：\r \r     ((2012-02-03,20:11:56,SampleClass5,[TRACE],verbose detail for id 990982084))\r     ((2012-02-03,20:11:56,SampleClass7,[TRACE],verbose detail for id 1560323914))\r     ((2012-02-03,20:11:56,SampleClass8,[DEBUG],detail for id 2083681507))\r     ((2012-02-03,20:11:56,SampleClass3,[TRACE],verbose detail for id 1718828806))\r     ((2012-02-03,20:11:56,SampleClass3,[INFO],everything normal for id 530537821))\r \r ## <a name=\"troubleshooting\"></a>故障排除\r \r ### <a name=\"errors-when-running-jobs\"></a>运行作业时出现错误\r \r 运行 hive 作业时，可能会遇到类似于以下文本的错误：\r \r     Caused by: org.apache.hadoop.hive.ql.metadata.HiveException: [Error 20001]: An error occurred while reading or writing to your custom script. It may have crashed with an error.\r \r 此问题可能是由 Python 文件中的行尾结束符号导致的。 许多 Windows 编辑器默认为使用 CRLF 作为行尾结束符号，但 Linux 应用程序通常应使用 LF。\r \r 可以使用 PowerShell 语句删除 CR 字符，然后再将文件上传到 HDInsight：\r \r ```powershell\r $original_file ='c:\\path\\to\\hiveudf.py'\r $text = [IO.File]::ReadAllText($original_file) -replace \"`r`n\", \"`n\"\r [IO.File]::WriteAllText($original_file, $text)\r ```\r \r ### <a name=\"powershell-scripts\"></a>PowerShell 脚本\r \r 用于运行示例的两个示例 PowerShell 脚本都包含一个带注释的行，该行显示作业的错误输出。 如果未看到作业的预期输出，请取消注释以下行，并查看错误信息中是否指明了问题。\r \r ```powershell\r # Get-AzureRmHDInsightJobOutput `\r         -Clustername $clusterName `\r         -JobId $job.JobId `\r         -HttpCredential $creds `\r         -DisplayOutputType StandardError\r ```\r \r 错误信息 (STDERR) 和作业的结果 (STDOUT) 也记录到 HDInsight 存储中。\r \r | 对于此作业... | 在 Blob 容器中查看这些文件 |\r | --- | --- |\r | Hive |/HivePython/stderr<p>/HivePython/stdout |\r | Pig |/PigPython/stderr<p>/PigPython/stdout |\r \r ## <a name=\"next\"></a>后续步骤\r \r 如果需要加载默认情况下未提供的 Python 模块，请参阅[如何将模块部署到 Azure HDInsight](http://blogs.msdn.com/b/benjguin/archive/2014/03/03/how-to-deploy-a-python-module-to-windows-azure-hdinsight.aspx)。\r \r 若要了解使用 Pig 和 Hive 的其他方式以及如何使用 MapReduce，请参阅以下文档：\r \r * [将 Hive 与 HDInsight 配合使用](hdinsight-use-hive.md)\r * [将 Pig 与 HDInsight 配合使用](hdinsight-use-pig.md)\r * [将 MapReduce 与 HDInsight 配合使用](hdinsight-use-mapreduce.md)\r \r \r <!--Update_Description: update wording and link references-->"}