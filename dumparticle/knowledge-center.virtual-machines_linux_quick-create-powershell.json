{"Title":"使用 PowerShell 创建 Linux 虚拟机","Description":"快速了解如何使用 PowerShell 创建 Linux 虚拟机","Content":"# <a name=\"create-a-linux-virtual-machine-with-powershell\"></a>使用 PowerShell 创建 Linux 虚拟机\r \r Azure PowerShell 模块用于从 PowerShell 命令行或脚本创建和管理 Azure 资源。 本指南详细介绍了如何使用 Azure PowerShell 模块部署运行 Ubuntu 服务器的虚拟机。 服务器部署以后，将创建 SSH 连接，并且安装 NGINX webserver。\r \r 如果没有 Azure 订阅，可在开始前创建一个[试用帐户](https://www.azure.cn/pricing/1rmb-trial/?WT.mc_id=A261C142F)。\r \r 本快速入门需要 Azure PowerShell 模块 3.6 或更高版本。 运行 ` Get-Module -ListAvailable AzureRM` 即可查找版本。 如果需要进行安装或升级，请参阅[安装 Azure PowerShell 模块](https://docs.microsoft.com/powershell/azure/install-azurerm-ps)。\r \r 最后，需要在 Windows 用户配置文件的 *.ssh* 目录中存储名为 *id_rsa.pub* 的公共 SSH 密钥。 有关创建适用于 Azure 的 SSH 密钥的详细信息，请参阅[创建适用于 Azure 的 SSH 密钥](mac-create-ssh-keys.md?toc=%2fvirtual-machines%2flinux%2ftoc.json)。\r \r ## <a name=\"log-in-to-azure\"></a>登录 Azure\r \r 使用 `Login-AzureRmAccount -EnvironmentName AzureChinaCloud` 命令登录到 Azure 订阅，并按照屏幕上的说明进行操作。\r \r ```powershell\r Login-AzureRmAccount -EnvironmentName AzureChinaCloud\r ```\r \r ## <a name=\"create-resource-group\"></a>创建资源组\r \r 使用 [New-AzureRmResourceGroup](https://docs.microsoft.com/powershell/module/azurerm.resources/new-azurermresourcegroup) 创建 Azure 资源组。 资源组是在其中部署和管理 Azure 资源的逻辑容器。\r \r ```powershell\r New-AzureRmResourceGroup -Name myResourceGroup -Location chinaeast\r ```\r \r ## <a name=\"create-networking-resources\"></a>创建网络资源\r \r 创建虚拟网络、子网和公共 IP 地址。 这些资源用来与虚拟机建立网络连接，以及连接到 Internet。\r \r ```powershell\r # Create a subnet configuration\r $subnetConfig = New-AzureRmVirtualNetworkSubnetConfig -Name mySubnet -AddressPrefix 192.168.1.0/24\r \r # Create a virtual network\r $vnet = New-AzureRmVirtualNetwork -ResourceGroupName myResourceGroup -Location chinaeast `\r -Name MYvNET -AddressPrefix 192.168.0.0/16 -Subnet $subnetConfig\r \r # Create a public IP address and specify a DNS name\r $pip = New-AzureRmPublicIpAddress -ResourceGroupName myResourceGroup -Location chinaeast `\r -AllocationMethod Static -IdleTimeoutInMinutes 4 -Name \"mypublicdns$(Get-Random)\"\r ```\r \r 创建网络安全组和网络安全组规则。 网络安全组使用入站和出站规则保护虚拟机。 在本例中，为端口 22 创建一个入站规则，该规则允许传入的 SSH 连接。 我们还需要为端口 80 创建入站规则，以允许传入的 Web 流量。\r \r ```powershell\r # Create an inbound network security group rule for port 22\r $nsgRuleSSH = New-AzureRmNetworkSecurityRuleConfig -Name myNetworkSecurityGroupRuleSSH  -Protocol Tcp `\r -Direction Inbound -Priority 1000 -SourceAddressPrefix * -SourcePortRange * -DestinationAddressPrefix * `\r -DestinationPortRange 22 -Access Allow\r \r # Create an inbound network security group rule for port 80\r $nsgRuleWeb = New-AzureRmNetworkSecurityRuleConfig -Name myNetworkSecurityGroupRuleWWW  -Protocol Tcp `\r -Direction Inbound -Priority 1001 -SourceAddressPrefix * -SourcePortRange * -DestinationAddressPrefix * `\r -DestinationPortRange 80 -Access Allow\r \r # Create a network security group\r $nsg = New-AzureRmNetworkSecurityGroup -ResourceGroupName myResourceGroup -Location chinaeast `\r -Name myNetworkSecurityGroup -SecurityRules $nsgRuleSSH,$nsgRuleWeb\r ```\r \r 使用 [New-AzureRmNetworkInterface](https://docs.microsoft.com/powershell/module/azurerm.network/new-azurermnetworkinterface) 为虚拟机创建网卡。 网卡将虚拟机连接到子网、网络安全组和公共 IP 地址。\r \r ```powershell\r # Create a virtual network card and associate with public IP address and NSG\r $nic = New-AzureRmNetworkInterface -Name myNic -ResourceGroupName myResourceGroup -Location chinaeast `\r -SubnetId $vnet.Subnets[0].Id -PublicIpAddressId $pip.Id -NetworkSecurityGroupId $nsg.Id\r ```\r \r ## <a name=\"create-virtual-machine\"></a>创建虚拟机\r \r 创建虚拟机配置。 此配置包括部署虚拟机时使用的设置，例如虚拟机映像、大小和身份验证配置。\r \r ```powershell\r # Define a credential object\r $securePassword = ConvertTo-SecureString ' ' -AsPlainText -Force\r $cred = New-Object System.Management.Automation.PSCredential (\"azureuser\", $securePassword)\r \r # Create a virtual machine configuration\r $vmConfig = New-AzureRmVMConfig -VMName myVM -VMSize Standard_D1 | `\r Set-AzureRmVMOperatingSystem -Linux -ComputerName myVM -Credential $cred -DisablePasswordAuthentication | `\r Set-AzureRmVMSourceImage -PublisherName Canonical -Offer UbuntuServer -Skus 16.04-LTS -Version latest | `\r Add-AzureRmVMNetworkInterface -Id $nic.Id\r \r # Configure SSH Keys\r $sshPublicKey = Get-Content \"$env:USERPROFILE\\.ssh\\id_rsa.pub\"\r Add-AzureRmVMSshPublicKey -VM $vmconfig -KeyData $sshPublicKey -Path \"/home/azureuser/.ssh/authorized_keys\"\r ```\r \r 使用 [New-AzureRmVM](https://docs.microsoft.com/powershell/module/azurerm.compute/new-azurermvm) 创建虚拟机。\r \r ```powershell\r New-AzureRmVM -ResourceGroupName myResourceGroup -Location chinaeast -VM $vmConfig\r ```\r \r ## <a name=\"connect-to-virtual-machine\"></a>连接到虚拟机\r \r 完成部署后，请与虚拟机建立 SSH 连接。\r \r 使用 [Get-AzureRmPublicIpAddress](https://docs.microsoft.com/powershell/module/azurerm.network/get-azurermpublicipaddress) 命令返回虚拟机的公共 IP 地址。\r \r ```powershell\r Get-AzureRmPublicIpAddress -ResourceGroupName myResourceGroup | Select IpAddress\r ```\r \r 在装有 SSH 的系统中，使用以下命令连接到虚拟机。 如果在 Windows 上操作，可以使用 [Putty](/virtual-machines/virtual-machines-linux-ssh-from-windows?toc=%2fvirtual-machines%2flinux%2ftoc.json#create-a-private-key-for-putty) 来创建连接。 \r \r ```bash \r ssh <Public IP Address>\r ```\r \r 出现提示时，请输入登录用户名 *azureuser*。 如果在创建 SSH 密钥时输入了密码，还需要输入此密码。\r \r ## <a name=\"install-nginx\"></a>安装 NGINX\r \r 使用以下命令更新包源并安装最新的 NGINX 包。 \r \r ```bash \r # update package source\r sudo apt-get -y update\r \r # install NGINX\r sudo apt-get -y install nginx\r ```\r \r ## <a name=\"view-the-ngix-welcome-page\"></a>查看 NGIX 欢迎页\r \r NGINX 已安装，并且现在已从 Internet 打开 VM 上的端口 80 - 可以使用所选的 Web 浏览器查看默认的 NGINX 欢迎页。 请务必使用前面记录的公共 IP 地址访问默认页面。 \r \r ![NGINX 默认站点](./media/quick-create-cli/nginx.png) \r \r ## <a name=\"clean-up-resources\"></a>清理资源\r \r 如果不再需要资源组、VM 和所有相关的资源，可以使用 [Remove-AzureRmResourceGroup](https://docs.microsoft.com/powershell/module/azurerm.resources/remove-azurermresourcegroup) 命令将其删除。\r \r ```powershell\r Remove-AzureRmResourceGroup -Name myResourceGroup\r ```\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r 在本快速入门中，部署了一个简单的虚拟机、一条网络安全组规则，并安装了一个 Web 服务器。 若要详细了解 Azure 虚拟机，请继续学习 Linux VM 的教程。\r \r > [!div class=\"nextstepaction\"]\r > [Azure Linux 虚拟机教程](./tutorial-manage-vm.md)\r \r <!--Update_Description: update cmdlet-->"}