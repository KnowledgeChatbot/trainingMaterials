{"Title":"如何使用服务管理 API (Python) - 功能指南","Description":"了解如何以编程方式执行 Python 提供的常见服务管理任务。","Content":"# <a name=\"how-to-use-service-management-from-python\"></a>如何从 Python 使用服务管理\r 本指南说明如何以编程方式从 Python 执行常见服务管理任务。 [用于 Python 的 Azure SDK](https://github.com/Azure/azure-sdk-for-python) 中的 ServiceManagementService 类支持以编程方式访问 [Azure 经典门户][management-portal]中提供的众多与服务管理相关的功能（例如创建、更新和删除云服务、部署、数据管理服务和虚拟机）。 此功能可用于构建需要以编程方式访问服务管理的应用程序。\r \r ## <a name=\"WhatIs\"> </a>什么是服务管理？\r 利用服务管理 API，可以编程方式访问通过 [Azure 经典管理门户][management-portal]提供的众多服务管理功能。 Azure SDK for Python 允许你管理云服务和存储帐户。\r \r 若要使用服务管理 API，需要[创建 Azure 帐户](https://www.azure.cn/pricing/1rmb-trial/)。\r \r ## <a name=\"Concepts\"> </a>概念\r Azure SDK for Python 可包装 [Azure 服务管理 API][svc-mgmt-rest-api]，后者是 REST API。所有 API 操作都通过 SSL 执行，并使用 X.509 v3 证书进行互相验证。可以从在 Azure 中运行的服务访问管理服务，或直接通过 Internet 从可发送 HTTPS 请求和接收 HTTPS 响应的任意应用程序访问管理服务。\r \r ## <a name=\"Installation\"> </a>安装\r `azure-servicemanagement-legacy` 包中提供本文介绍的所有功能，可以使用 pip 安装该包。 有关安装的详细信息（例如，如果刚开始接触 Python），请参阅此文：[安装 Python 和 Azure SDK](../python-how-to-install.md)\r \r ## <a name=\"Connect\"> </a>如何：连接到服务管理\r 若要连接到服务管理终结点，你需要 Azure 订阅 ID 和有效管理证书。可以通过 [Azure 经典管理门户][management-portal]获取订阅 ID。\r \r > [!NOTE]\r > 现在，在 Windows 上运行时可以使用通过 OpenSSL 创建的证书。  需要 Python 2.7.4 或更高版本。 我们建议用户使用 OpenSSL 而不是 .pfx，因为将来可能会取消对 .pfx 证书的支持。\r > \r > \r \r ### <a name=\"management-certificates-on-windowsmaclinux-openssl\"></a>Windows/Mac/Linux 上的管理证书 (OpenSSL)\r 可使用 [OpenSSL](http://www.openssl.org/) 创建管理证书。  实际上需要创建两个证书，一个用于服务器（`.cer` 文件），一个用于客户端（`.pem` 文件）。 若要创建 `.pem` 文件，请执行以下代码：\r \r ```\r openssl req -x509 -nodes -days 365 -newkey rsa:1024 -keyout mycert.pem -out mycert.pem\r ```\r \r 若要创建 `.cer` 证书，请执行以下代码：\r \r ```\r openssl x509 -inform pem -in mycert.pem -outform der -out mycert.cer\r ```\r \r 有关 Azure 证书的详细信息，请参阅 [Azure 云服务证书概述](./cloud-services-certs-create.md)。 有关 OpenSSL 参数的完整说明，请参阅 [http://www.openssl.org/docs/apps/openssl.html](http://www.openssl.org/docs/apps/openssl.html)上的文档。\r \r 创建这些文件后，需要通过 [Azure 经典管理门户][management-portal]中“设置”选项卡的“上传”操作将 `.cer` 文件上传到 Azure，并且需要记下 `.pem` 文件的保存位置。\r \r 在获取订阅 ID、创建证书并将 `.cer` 文件上传到 Azure 之后，可通过将订阅 ID 和 `.pem` 文件的路径传递给 ServiceManagementService 来连接到 Azure 管理终结点：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r subscription_id = '<your_subscription_id>'\r certificate_path = '<path_to_.pem_certificate>'\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r ```\r \r 在前面的示例中，`sms` 是一个 **ServiceManagementService** 对象。**ServiceManagementService** 类是用于管理 Azure 服务的主类。\r \r ### <a name=\"management-certificates-on-windows-makecert\"></a>Windows 上的管理证书 (MakeCert)\r \r 可使用 `makecert.exe`在计算机上创建自签名管理证书。  以“管理员”身份打开“Visual Studio 命令提示符”并且使用以下命令，用你要使用的证书名称替换 AzureCertificate。\r \r ```\r makecert -sky exchange -r -n \"CN=AzureCertificate\" -pe -a sha1 -len 2048 -ss My \"AzureCertificate.cer\"\r ```\r \r 该命令将创建 `.cer` 文件，然后将该文件安装在“个人”证书存储中。 有关详细信息，请参阅 [Azure 云服务证书概述](cloud-services-certs-create.md)。\r \r 创建证书后，需要通过[管理门户][management-portal]的“设置”选项卡的“上传”操作，将 `.cer` 文件上传到 Azure。\r \r 在获取订阅 ID、创建证书并将 `.cer` 文件上传到 Azure 后，可通过将订阅 ID 以及你的“个人”证书存储中的证书位置传递到 ServiceManagementService（此外，用你的证书名称替代 AzureCertificate），来连接到 Azure 管理终结点：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r subscription_id = '<your_subscription_id>'\r certificate_path = 'CURRENT_USER\\\\my\\\\AzureCertificate'\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r ```\r \r 在前面的示例中， `sms` 是一个 **ServiceManagementService** 对象。 **ServiceManagementService** 类是用于管理 Azure 服务的主类。\r \r ## <a name=\"ListAvailableLocations\"> </a>如何：列出可用位置\r \r 若要列出可用于托管服务的位置，请使用 list\\_locations 方法：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r result = sms.list_locations()\r for location in result:\r     print(location.name)\r ```\r \r 创建云服务或存储服务时，需要提供有效位置。 list\\_locations 方法将始终返回当前可用位置的最新列表。 截止到本文撰写时为止，可用位置为：\r \r - 中国东部 \r - 中国北部\r \r ## <a name=\"CreateCloudService\"> </a>如何：创建云服务\r \r 创建应用程序以及在 Azure 中运行该应用程序时，相关代码和配置统称为 Azure [云服务][]（在早期 Azure 版本中称为托管服务）。 create\\_hosted\\_service 方法允许你通过提供托管服务名称（它在 Azure 中必须唯一）、标签（自动编码为 base64）、说明和位置来创建新的托管服务。\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r name = 'myhostedservice'\r label = 'myhostedservice'\r desc = 'my hosted service'\r location = 'China East'\r \r sms.create_hosted_service(name, label, desc, location)\r ```\r \r 可使用 list\\_hosted\\_services 方法列出订阅的所有托管服务：\r \r ```\r result = sms.list_hosted_services()\r \r for hosted_service in result:\r     print('Service name: ' + hosted_service.service_name)\r     print('Management URL: ' + hosted_service.url)\r     print('Location: ' + hosted_service.hosted_service_properties.location)\r     print('')\r ```\r \r 如果希望获得有关特定托管服务的信息，可以通过将托管服务名称传递给 get\\_hosted\\_service\\_properties 方法来实现此目的：\r \r ```\r hosted_service = sms.get_hosted_service_properties('myhostedservice')\r \r print('Service name: ' + hosted_service.service_name)\r print('Management URL: ' + hosted_service.url)\r print('Location: ' + hosted_service.hosted_service_properties.location)\r ```\r \r 创建云服务后，可使用 create\\_deployment 方法将代码部署到服务。\r \r ## <a name=\"DeleteCloudService\"> </a>如何：删除云服务\r \r 可通过将服务名称传递给delete\\_hosted\\_service  方法来删除云服务：\r \r ```\r sms.delete_hosted_service('myhostedservice')\r ```\r \r 必须先删除服务的所有部署，然后才能删除服务。 （有关详细信息，请参阅[如何：删除部署](#DeleteDeployment)。）\r \r ## <a name=\"DeleteDeployment\"> </a>如何：删除部署\r \r 若要删除部署，请使用 delete\\_deployment 方法。 下面的示例演示如何删除名为 `v1`的部署。\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r sms.delete_deployment('myhostedservice', 'v1')\r ```\r \r ## <a name=\"CreateStorageService\"> </a>如何：创建存储服务\r \r 利用[存储服务](../storage/common/storage-create-storage-account.md)，可以访问 Azure [Blob](../storage/blobs/storage-python-how-to-use-blob-storage.md)、[表](../cosmos-db/table-storage-how-to-use-python.md)和[队列](../storage/queues/storage-python-how-to-use-queue-storage.md)。 若要创建存储服务，需要提供服务名称（3 至 24 个小写字符且在 Azure 中唯一）、说明、标签（最多 100 个字符，自动编码为 base64）以及位置。 下面的示例演示如何通过指定位置来创建存储服务。\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r name = 'mystorageaccount'\r label = 'mystorageaccount'\r location = 'China East'\r desc = 'My storage account description.'\r \r result = sms.create_storage_account(name, desc, label, location=location)\r \r operation_result = sms.get_operation_status(result.request_id)\r print('Operation status: ' + operation_result.status)\r ```\r \r 请注意，在上面的示例中，可以通过将 create\\_storage\\_account 返回的结果传递到 get\\_operation\\_status 方法来检索 create\\_storage\\_account 操作的状态。  \r \r 可使用 list\\_storage\\_accounts 方法列出存储帐户及其属性：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r result = sms.list_storage_accounts()\r for account in result:\r     print('Service name: ' + account.service_name)\r     print('Location: ' + account.storage_service_properties.location)\r     print('')\r ```\r \r ## <a name=\"DeleteStorageService\"> </a>如何：删除存储服务\r \r 可通过将存储服务名称传递给 delete\\_storage\\_account 方法来删除存储服务。 删除存储服务会删除该服务中存储的所有数据（Blob、表和队列）。\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r sms.delete_storage_account('mystorageaccount')\r ```\r \r ## <a name=\"ListOperatingSystems\"> </a>如何：列出可用操作系统\r \r 若要列出可用于托管服务的操作系统，请使用 list\\_operating\\_systems 方法：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r result = sms.list_operating_systems()\r \r for os in result:\r     print('OS: ' + os.label)\r     print('Family: ' + os.family_label)\r     print('Active: ' + str(os.is_active))\r ```\r \r 或者，也可使用 list\\_operating\\_system\\_families 方法，按系列对操作系统进行分组：\r \r ```\r result = sms.list_operating_system_families()\r \r for family in result:\r     print('Family: ' + family.label)\r     for os in family.operating_systems:\r         if os.is_active:\r             print('OS: ' + os.label)\r             print('Version: ' + os.version)\r     print('')\r ```\r \r ## <a name=\"CreateVMImage\"> </a>如何：创建操作系统映像\r \r 若要将操作系统映像添加到映像存储库中，请使用 add\\_os\\_image 方法：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r name = 'mycentos'\r label = 'mycentos'\r os = 'Linux' # Linux or Windows\r media_link = 'url_to_storage_blob_for_source_image_vhd'\r \r result = sms.add_os_image(label, media_link, name, os)\r \r operation_result = sms.get_operation_status(result.request_id)\r print('Operation status: ' + operation_result.status)\r ```\r \r 若要列出可用的操作系统映像，请使用 list\\_os\\_images 方法。 这包括所有平台映像和用户映像：\r \r ```\r result = sms.list_os_images()\r \r for image in result:\r     print('Name: ' + image.name)\r     print('Label: ' + image.label)\r     print('OS: ' + image.os)\r     print('Category: ' + image.category)\r     print('Description: ' + image.description)\r     print('Location: ' + image.location)\r     print('Media link: ' + image.media_link)\r     print('')\r ```\r \r ## <a name=\"DeleteVMImage\"> </a>如何：删除操作系统映像\r \r 若要删除用户映像，请使用 delete\\_os\\_image 方法：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r result = sms.delete_os_image('mycentos')\r \r operation_result = sms.get_operation_status(result.request_id)\r print('Operation status: ' + operation_result.status)\r ```\r \r ## <a name=\"CreateVM\"> </a>如何：创建虚拟机\r \r 若要创建虚拟机，首先需要创建 [云服务](#CreateCloudService)。  然后使用 create\\_virtual\\_machine\\_deployment 方法创建虚拟机部署：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r name = 'myvm'\r location = 'China East'\r \r #Set the location\r sms.create_hosted_service(service_name=name,\r     label=name,\r     location=location)\r \r # Name of an os image as returned by list_os_images\r image_name = 'OpenLogic__OpenLogic-CentOS-62-20120531-zh-cn-30GB.vhd'\r \r # Destination storage account container/blob where the VM disk\r # will be created\r media_link = 'url_to_target_storage_blob_for_vm_hd'\r \r # Linux VM configuration, you can use WindowsConfigurationSet\r # for a Windows VM instead\r linux_config = LinuxConfigurationSet('myhostname', 'myuser', 'mypassword', True)\r \r os_hd = OSVirtualHardDisk(image_name, media_link)\r \r sms.create_virtual_machine_deployment(service_name=name,\r     deployment_name=name,\r     deployment_slot='production',\r     label=name,\r     role_name=name,\r     system_config=linux_config,\r     os_virtual_hard_disk=os_hd,\r     role_size='Small')\r ```\r \r ## <a name=\"DeleteVM\"> </a>如何：删除虚拟机\r \r 若要删除虚拟机，请首先使用 delete\\_deployment 方法删除部署：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r sms.delete_deployment(service_name='myvm',\r     deployment_name='myvm')\r ```\r \r 然后可以使用 delete\\_hosted\\_service 方法删除云服务：\r \r ```\r sms.delete_hosted_service(service_name='myvm')\r ```\r \r ##<a name=\"how-to-create-a-virtual-machine-from-a-captured-virtual-machine-image\"></a>如何：基于捕获的虚拟机映像创建虚拟机\r \r 若要捕获 VM 映像，请先调用 capture\\_vm\\_image 方法：\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r # replace the below three parameters with actual values\r hosted_service_name = 'hs1'\r deployment_name = 'dep1'\r vm_name = 'vm1'\r \r image_name = vm_name + 'image'\r image = CaptureRoleAsVMImage\t('Specialized',\r     image_name,\r     image_name + 'label',\r     image_name + 'description',\r     'english',\r     'mygroup')\r \r result = sms.capture_vm_image(\r         hosted_service_name,\r         deployment_name,\r         vm_name,\r         image\r     )\r ```\r \r 接下来，为确保成功捕获了映像，请使用 **list\\_vm\\_images** API 并确保映像显示在结果中：\r \r ```\r images = sms.list_vm_images()\r ```\r \r 为了最后使用捕获的映像创建虚拟机，请像前面一样使用 create\\_virtual\\_machine\\_deployment 方法，不过这次改为传入 vm_image_name\r \r ```\r from azure import *\r from azure.servicemanagement import *\r \r sms = ServiceManagementService(subscription_id, certificate_path)\r \r name = 'myvm'\r location = 'China North'\r \r #Set the location\r sms.create_hosted_service(service_name=name,\r     label=name,\r     location=location)\r \r sms.create_virtual_machine_deployment(service_name=name,\r     deployment_name=name,\r     deployment_slot='production',\r     label=name,\r     role_name=name,\r     system_config=linux_config,\r     os_virtual_hard_disk=None,\r     role_size='Small',\r     vm_image_name = image_name)\r ```\r \r 若要了解有关如何捕获 Linux 虚拟机的详细信息，请参阅[如何捕获 Linux 虚拟机。](../virtual-machines/virtual-machines-linux-classic-capture-image.md)\r \r 若要了解有关如何捕获 Windows 虚拟机的详细信息，请参阅[如何捕获 Windows 虚拟机。](../virtual-machines/virtual-machines-windows-classic-capture-image.md)\r \r ## <a name=\"What's Next\"></a>后续步骤\r \r 既已了解有关服务管理的基础知识，现可访问 [Azure Python SDK 的完整 API 参考文档](http://azure-sdk-for-python.readthedocs.org/)，轻松执行复杂任务来管理 Python 应用程序。\r \r 有关详细信息，请参阅 [Python 开发人员中心](https://www.azure.cn/develop/python/)。\r \r [What is Service Management]: #WhatIs\r [Concepts]: #Concepts\r [How to: Connect to service management]: #Connect\r [How to: List available locations]: #ListAvailableLocations\r [How to: Create a cloud service]: #CreateCloudService\r [How to: Delete a cloud service]: #DeleteCloudService\r [How to: Create a deployment]: #CreateDeployment\r [How to: Update a deployment]: #UpdateDeployment\r [How to: Move deployments between staging and production]: #MoveDeployments\r [How to: Delete a deployment]: #DeleteDeployment\r [How to: Create a storage service]: #CreateStorageService\r [How to: Delete a storage service]: #DeleteStorageService\r [How to: List available operating systems]: #ListOperatingSystems\r [How to: Create an operating system image]: #CreateVMImage\r [How to: Delete an operating system image]: #DeleteVMImage\r [How to: Create a virtual machine]: #CreateVM\r [How to: Delete a virtual machine]: #DeleteVM\r [Next Steps]: #NextSteps\r [management-portal]: https://manage.windowsazure.cn/\r [svc-mgmt-rest-api]: http://msdn.microsoft.com/zh-cn/library/windowsazure/ee460799.aspx\r \r [云服务]: /cloud-services/\r \r <!--Update_Description:update meta properties and wording-->"}