{"Title":"在 Azure Linux 虚拟机上安装 MySQL","Description":"了解如何在 Azure Linux 虚拟机上安装 MySQL","Content":"\r # 在 Azure Linux 虚拟机上安装 MySQL\r \r ##目录\r \r - [Redhat base Linux 下安装 MySQL Server 5.6](#install-mysql-server-5_6-under-redhat-base-linux)\r - [Ubuntu Linux 下安装 MySQL Server 5.6](#install-mysql-server-5_6-under-ubuntu-linux)\r - [SUSE Linux 下安装 MySQL Server 5.6](#install-mysql-server-5_6-under-suse-linux)\r \r MySQL 有多个稳定版本以及多种安装方式，这篇文档以 MySQL 5.6， repository package 安装方式演示整个安装过程。\r \r 连接到您的 LINUX 虚拟机。如果这是您第一次使用 Azure 的 LINUX 虚拟机，请参考 [Azure Linux VM tutorial](/virtual-machines/virtual-machines-linux-quick-create-portal) 连接到虚拟机。\r \r 不同的 LINUX 发行版在安装 MySQL 时有少许的不同。请根据您的 LINUX 版本选择对应的步骤。\r \r ##<a name=\"install-mysql-server-5_6-under-redhat-base-linux\"></a> Redhat base Linux 下安装 MySQL Server 5.6: (以 CentOS 7.0, 64-bit system, MySQL Server 5.6 为例)\r \r 1. 下载 MySQL yum repository\r \r     ```\r     $sudo wget http://repo.mysql.com/mysql-community-release-el6-5.noarch.rpm\r     ```\r \r 2. 安装此 release package\r \r     ```\r     $sudo yum localinstall -y mysql-community-release-el6-5.noarch.rpm\r     ```\r \r 3. 安装 MySQL Server 5.6\r \r     ```\r     $sudo yum install mysql-community-server -y\r     ```\r \r     注意，由于网速等原因可能会导致下载安装包时等待时间过多而安装失败的情况， 没有关系，只需重复此命令安装即可。\r \r 4. 启动 MySQL Server 5.6\r \r     ```\r     $sudo service mysqld start\r     ```\r \r 5. 设置 MySQL root 密码。安装后默认 root 密码为空。第一次使用时最好设置 root 密码\r \r     ```\r     $sudo mysqladmin -u root password <password>\r     ```\r \r     其中 <password> 是您要设置的 root 密码\r \r 6. 如果需要更新 root 密码的话，有好几种方式。这里还是用 mysqladmin 命令\r \r     ```\r     $sudo mysqladmin -uroot -p<password> password\r     ```\r \r     其中 <password> 是您原先的 root 密码，敲入上述命令后回车，输入新密码即可。\r \r 7. 停止 MySQL 服务\r \r     ```\r     $sudo service mysqld stop\r     ```\r \r 8. 配置开机自启动\r \r     ```\r     $sudo chkconfig mysqld on             #CentOS 7.0 亦可使用此命令\r     ```\r \r 9. 授权其他机器访问。默认 root 只有本机访问。\r \r     ```\r     $mysql -uroot -p\r     mysql>grant all privileges on *.* to 'root'@'10.0.0.1' identified by '123456';\r     mysql>flush privileges;\r     mysql>exit\r     ```\r \r     注意上面 10.0.0.1 是被授权访问此 MySQL Server 的机器地址，123456 是您 root 密码。如果用 % 替代 IP 地址的话表明所有机器都可访问此数据库服务器。\r \r 10. 打开 3306 端口。3306 端口是 MySQL 的默认端口。请参考[创建终结点](/virtual-machines/virtual-machines-linux-classic-setup-endpoints)打开端口\r 11. 此时可以从 10.0.0.1 机器远程访问您的 MySQL 数据库了。\r \r ##<a name=\"install-mysql-server-5_6-under-ubuntu-linux\"></a> Ubuntu Linux 下安装 MySQL Server 5.6: (以 Ubuntu 14.04, 64-bit system, MySQL 5.6 为例)\r \r 1. 安装 MySQL Server 5.6\r \r     ```\r     $sudo apt-get update\r     $sudo apt-get -y install mysql-server-5.6\r     ```\r \r     在安装时会出现类似下面窗口要求您设置 root 密码\r \r     ![install-ubuntu](./media/open-source-azure-virtual-machines-linux-install-mysql/install-ubuntu.png)\r \r     输入密码，再次确认后即可。\r \r 2. 访问。MySQL Server 安装完后会自动启动，因此我们可以直接访问\r \r     ```\r     $mysql -uroot -p\r     ```\r \r     输入 root 密码即可登录访问\r \r 3. 停止 MySQL 服务\r \r     ```\r     $sudo service mysql stop\r     ```\r \r 4. 启动 MySQL 服务\r \r     ```\r     $sudo service mysql start\r     ```\r \r 5. 授权其他机器访问\r \r     ```\r     $ sudo sed -i 's/^bind-address/#bind-address/' /etc/mysql/my.cnf\r     $mysql -uroot -p\r     mysql>grant all privileges on *.* to 'root'@'10.0.0.1' identified by '123456';\r     mysql>flush privileges;\r     mysql>exit\r     ```\r \r     注意上面 10.0.0.1 是被授权访问此 MySQL Server 的机器地址，123456 是您 root 密码。如果用%替代 IP 地址的话表明所有机器都可访问此数据库服务器。\r \r 6. 打开 3306 端口。3306 端口是 MySQL 的默认端口。请参考[创建终结点](/virtual-machines/virtual-machines-linux-classic-setup-endpoints)打开端口 \r \r 7. 此时可以从 10.0.0.1 机器远程访问您的 MySQL 数据库了。\r \r ##<a name=\"install-mysql-server-5_6-under-suse-linux\"></a> SUSE Linux 下安装 MySQL Server 5.6: (以 SLES 12, 64-bit system, MySQL Server 5.6 为例)\r \r 1. 下载 repository package\r \r     ```\r     wget http://dev.mysql.com/get/mysql57-community-release-sles12-7.noarch.rpm\r     ```\r \r 2. 安装 repo package\r \r     ```\r     $sudo rpm -ivh mysql57-community-release-sles12-7.noarch.rpm\r     $sudo rpm --import /etc/RPM-GPG-KEY-mysql\r     $sudo zypper modifyrepo -d mysql57-community\r     $sudo zypper modifyrepo -e mysql56-community\r     $sudo zypper refresh\r     ```\r \r 3. 安装 MySQL Server 5.6\r \r     ```\r     $sudo zypper install -y mysql-community-server\r     ```\r \r     注意，如果由于网速等原因在下载时时间过长导致安装失败，没有关系，重复此命令安装即可。\r \r 4. 开机自启动\r \r     ```\r     $sudo systemctl enable mysql\r     ```\r \r 5. 启动服务\r \r     ```\r     $sudo systemctl start mysql\r     ```\r \r 6. 设置 root 密码\r \r     ```\r     $sudo mysqladmin -u root password <password>\r     ```\r \r     其中 <password> 是您要设置的 root 密码\r \r 7. 授权其他机器访问。默认 root 只有本机访问。\r \r     ```\r     $mysql -uroot -p\r     mysql>grant all privileges on *.* to 'root'@'10.0.0.1' identified by '123456';\r     mysql>flush privileges;\r     mysql>exit\r     ```\r \r     注意上面 10.0.0.1 是被授权访问此 MySQL Server 的机器地址，123456 是您 root 密码。如果用%替代 IP 地址的话表明所有机器都可访问此数据库服务器。\r \r 8. 打开 3306 端口。3306 端口是 MySQL 的默认端口。请参考[创建终结点](/virtual-machines/virtual-machines-linux-classic-setup-endpoints)打开端口 \r \r 9. 此时可以从 10.0.0.1 机器远程访问您的 MySQL 数据库了。\r \r 更多资料请参考[官网](http://dev.mysql.com/doc/refman/5.6/en/)"}