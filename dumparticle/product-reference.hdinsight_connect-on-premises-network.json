{"Title":"将 HDInsight 连接到本地网络 - Azure HDInsight","Description":"了解如何在 Azure 虚拟网络中创建 HDInsight 群集，然后将其连接到本地网络。 了解如何使用自定义 DNS 服务器在 HDInsight 和本地网络之间配置名称解析。","Content":"# <a name=\"connect-hdinsight-to-your-on-premise-network\"></a>将 HDInsight 连接到本地网络\r \r 了解如何使用 Azure 虚拟网络和 VPN 网关将 HDInsight 连接到本地网络。 本文档提供以下相关规划信息：\r \r * 在连接到本地网络的 Azure 虚拟网络上使用 HDInsight。\r \r * 配置虚拟网络与本地网络之间的 DNS 名称解析。\r \r * 将网络安全组配置为限制对 HDInsight 的 Internet 访问。\r \r * 虚拟网络上由 HDInsight 提供的端口。\r \r ## <a name=\"create-the-virtual-network-configuration\"></a>创建虚拟网络配置\r \r 请参阅以下文档，了解如何创建连接到本地网络的 Azure 虚拟网络：\r \r * [使用 Azure 门户](../vpn-gateway/vpn-gateway-howto-site-to-site-resource-manager-portal.md)\r \r * [使用 Azure PowerShell](../vpn-gateway/vpn-gateway-create-site-to-site-rm-powershell.md)\r \r * [使用 Azure CLI](../vpn-gateway/vpn-gateway-howto-site-to-site-resource-manager-cli.md)\r \r ## <a name=\"configure-name-resolution\"></a>配置名称解析\r \r 若要让 HDInsight 和已连接网络中的资源通过名称通信，必须执行以下操作：\r \r * 在 Azure 虚拟网络中创建自定义 DNS 服务器。\r \r * 将虚拟网络配置为使用自定义 DNS 服务器而非默认的 Azure 递归解析程序。\r \r * 配置自定义 DNS 服务器和本地 DNS 服务器之间的转发。\r \r 该配置启用以下行为：\r \r * 将完全限定的域名（其中包含虚拟网络的 DNS 后缀）的请求转发到自定义 DNS 服务器。 自定义 DNS 服务器然后会将这些请求转发到 Azure 递归解析程序，由后者返回 IP 地址。\r \r * 将所有其他请求转发到本地 DNS 服务器。 甚至对公共 Internet 资源（如 microsoft.com）的请求也将因名称解析转发至本地 DNS 服务器。\r \r 在下面的关系图中，绿线表示以虚拟网络的 DNS 后缀结尾的资源请求。 蓝线表示本地网络或公共 Internet 上的资源请求。\r \r ![本文档将使用如何解析配置中的 DNS 请求的关系图](./media/connect-on-premises-network/on-premises-to-cloud-dns.png)\r \r ### <a name=\"create-a-custom-dns-server\"></a>创建自定义 DNS 服务器\r \r > [!IMPORTANT]\r > 必须先创建并配置 DNS 服务器，此后再将 HDInsight 安装到虚拟网络中。\r \r 若要创建使用 [Bind](https://www.isc.org/downloads/bind/) DNS 软件的 Linux VM，请执行以下步骤：\r \r > [!NOTE]\r > 以下步骤使用 [Azure 门户](https://portal.azure.cn)来创建 Azure 虚拟机。 有关创建虚拟机的其他方法，请参阅以下文档：\r >\r > * [创建 VM - Azure CLI](../virtual-machines/linux/quick-create-cli.md)\r > * [创建 VM - Azure PowerShell](../virtual-machines/linux/quick-create-portal.md)\r \r 1. 在 [Azure 门户](https://portal.azure.cn)中，依次选择“+”、“计算”和“Ubuntu Server 16.04 LTS”。\r \r     ![创建 Ubuntu 虚拟机](./media/connect-on-premises-network/create-ubuntu-vm.png)\r \r 2. 在“基本信息”部分输入以下信息：\r \r     * 名称：用于标识此虚拟机的友好名称。 例如，DNSProxy。\r     * 用户名：SSH 帐户的名称。\r     * SSH 公钥或密码：SSH 帐户的身份验证方法。 建议使用公钥，因为更安全。 有关详细信息，请参阅[为 Linux VM 创建和使用 SSH 密钥](../virtual-machines/linux/mac-create-ssh-keys.md)文档。\r     * 资源组：选择“使用现有资源组”，然后选择包含此前创建的虚拟网络的资源组。\r     * 位置：选择虚拟网络所在的位置。\r \r     ![虚拟机基本配置](./media/connect-on-premises-network/vm-basics.png)\r \r     将其他项保留默认值，然后选择“确定”。\r \r 3. 在“选择大小”部分，选择 VM 大小。 就本教程来说，请选择规模最小且成本最低的选项。 若要继续，请使用“选择”按钮。\r \r 4. 在“设置”部分输入以下信息：\r \r     * 虚拟网络：选择此前创建的虚拟网络。\r \r     * 子网：选择虚拟网络的默认子网。 请勿选择 VPN 网关使用的子网。\r \r     * 诊断存储帐户：选择现有的存储帐户，或新建一个。\r \r     ![虚拟网络设置](./media/connect-on-premises-network/virtual-network-settings.png)\r \r     将其他项保留默认值，然后选择“确定”继续。\r \r 5. 在“购买”部分，选择“购买”按钮可创建虚拟机。\r \r 6. 创建虚拟机后，即会显示其“概述”部分。 从左侧列表中选择“属性”。 保存“公共 IP 地址”和“专用 IP 地址”值。 下一部分会用到它。\r \r     ![公共和专用 IP 地址](./media/connect-on-premises-network/vm-ip-addresses.png)\r \r ### <a name=\"install-and-configure-bind-dns-software\"></a>安装和配置 Bind（DNS 软件）\r \r 1. 使用 SSH 连接到虚拟机的公共 IP 地址。 以下示例将在 40.68.254.142 连接到虚拟机：\r \r     ```bash\r     ssh sshuser@40.68.254.142\r     ```\r \r     将 `sshuser` 替换为创建群集时指定的 SSH 用户帐户。\r \r     > [!NOTE]\r     > 可通过多种方法获取 `ssh` 实用工具。 在 Linux、Unix 和 macOS 上，操作系统会附带此实用工具。 如果使用的是 Windows，请考虑以下选项之一：\r \r     > * [Windows 10 版 Bash on Ubuntu](https://msdn.microsoft.com/commandline/wsl/about)\r     > * [Git (https://git-scm.com/)](https://git-scm.com/)\r     > * [OpenSSH (https://github.com/PowerShell/Win32-OpenSSH/wiki/Install-Win32-OpenSSH)](https://github.com/PowerShell/Win32-OpenSSH/wiki/Install-Win32-OpenSSH)\r \r 2. 若要安装 Bind，请从 SSH 会话中使用以下命令：\r \r     ```bash\r     sudo apt-get update -y\r     sudo apt-get install bind9 -y\r     ```\r \r 3. 若要配置 Bind，以便将名称解析请求转发到本地 DNS 服务器，请使用以下文本作为 `/etc/bind/named.conf.options` 文件的内容：\r \r         acl goodclients {\r             10.0.0.0/16; # Replace with the IP address range of the virtual network\r             10.1.0.0/16; # Replace with the IP address range of the on-premises network\r             localhost;\r             localnets;\r         };\r \r         options {\r                 directory \"/var/cache/bind\";\r \r                 recursion yes;\r \r                 allow-query { goodclients; };\r \r                 forwarders {\r                 192.168.0.1; # Replace with the IP address of the on-premises DNS server\r                 };\r \r                 dnssec-validation auto;\r \r                 auth-nxdomain no;    # conform to RFC1035\r                 listen-on { any; };\r         };\r \r     > [!IMPORTANT]\r     > 将 `goodclients` 节中的值替换为虚拟网络和本地网络的 IP 地址范围。 此节定义此 DNS 服务器从其接受请求的地址。\r     >\r     > 将 `forwarders` 节中的 `192.168.0.1` 项替换为本地 DNS 服务器的 IP 地址。 此项将 DNS 请求路由到本地 DNS 服务器进行解析。\r \r     若要编辑该文件，请使用以下命令：\r \r     ```bash\r     sudo nano /etc/bind/named.conf.options\r     ```\r \r     若要保存文件，请使用 Ctrl+X、Y，然后按 Enter。\r \r 4. 在 SSH 会话中使用以下命令：\r \r     ```bash\r     hostname -f\r     ```\r \r     此命令返回类似于以下文本的值：\r \r         dnsproxy.icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn\r \r     `icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn` 文本是此虚拟网络的 DNS 后缀。 保存该值，因为以后会用到。\r \r 5. 若要配置 Bind，以便为虚拟网络中的资源解析 DNS 名称，请使用以下文本作为 `/etc/bind/named.conf.local` 文件的内容：\r \r         // Replace the following with the DNS suffix for your virtual network\r         zone \"icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn\" {\r             type forward;\r             forwarders {168.63.129.16;}; # The Azure recursive resolver\r         };\r \r     > [!IMPORTANT]\r     > 必须将 `icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn` 替换为此前检索的 DNS 后缀。\r \r     若要编辑该文件，请使用以下命令：\r \r     ```bash\r     sudo nano /etc/bind/named.conf.local\r     ```\r \r     若要保存文件，请使用 Ctrl+X、Y，然后按 Enter。\r \r 6. 若要启动 Bind，请使用以下命令：\r \r     ```bash\r     sudo service bind9 restart\r     ```\r \r 7. 若要验证 Bind 能否解析本地网络中资源的名称，请使用以下命令：\r \r     ```bash\r     sudo apt install dnsutils\r     nslookup dns.mynetwork.net 10.0.0.4\r     ```\r \r     > [!IMPORTANT]\r     > 将 `dns.mynetwork.net` 替换为本地网络中资源的完全限定的域名 (FQDN)。\r     >\r     > 将 `10.0.0.4` 替换为虚拟网络中自定义 DNS 服务器的内部 IP 地址。\r \r     显示的响应类似于以下文本：\r \r         Server:         10.0.0.4\r         Address:        10.0.0.4#53\r \r         Non-authoritative answer:\r         Name:   dns.mynetwork.net\r         Address: 192.168.0.4\r \r ### <a name=\"configure-the-virtual-network-to-use-the-custom-dns-server\"></a>将虚拟网络配置为使用自定义 DNS 服务器\r \r 若要将虚拟网络配置为使用自定义 DNS 服务器而非 Azure 递归解析程序，请执行以下步骤：\r \r 1. 在 [Azure 门户](https://portal.azure.cn)中选择虚拟网络，然后选择“DNS 服务器”。\r \r 2. 选择“自定义”，并输入自定义 DNS 服务器的内部 IP 地址。 最后，选择“保存”。\r \r     ![设置网络的自定义 DNS 服务器](./media/connect-on-premises-network/configure-custom-dns.png)\r \r ### <a name=\"configure-the-on-premises-dns-server\"></a>配置本地 DNS 服务器\r \r 前一部分已将自定义 DNS 服务器配置为将请求转发到本地 DNS 服务器。 接下来，必须将本地 DNS 服务器配置为将请求转发到自定义 DNS 服务器。\r \r 有关 DNS 服务器配置的具体步骤，请参阅 DNS 服务器软件的文档。 请查找配置条件转发器的步骤。\r \r 条件转发仅转发对特定 DNS 后缀的请求。 在此示例中，必须为虚拟网络的 DNS 后缀配置转发器。 对此后缀的请求应转发到自定义 DNS 服务器的 IP 地址。 \r \r 以下文本是 Bind DNS 软件的条件转发器配置的示例：\r \r     zone \"icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn\" {\r         type forward;\r         forwarders {10.0.0.4;}; # The custom DNS server's internal IP address\r     };\r \r 若要了解如何在 Windows Server 2016 上使用 DNS，请参阅 [Add-DnsServerConditionalForwarderZone](https://technet.microsoft.com/itpro/powershell/windows/dnsserver/add-dnsserverconditionalforwarderzone) 文档。\r \r 配置本地 DNS 服务器以后，即可在本地网络中使用 `nslookup` 来验证能否解析虚拟网络中的名称。 下面为示例 \r \r ```bash\r nslookup dnsproxy.icb0d0thtw0ebifqt0g1jycdxd.ex.internal.chinacloudapp.cn 196.168.0.4\r ```\r \r 该示例使用位于 196.168.0.4 的本地 DNS 服务器来解析自定义 DNS 服务器的名称。 将该 IP 地址替换为本地 DNS 服务器的 IP 地址。 将 `dnsproxy` 地址替换为自定义 DNS 服务器的完全限定的域名。\r \r ## <a name=\"optional-control-network-traffic\"></a>可选：控制网络流量\r \r 可以使用网络安全组 (NSG) 或用户定义路由 (UDR) 来控制网络通信。 NSG 用于筛选入站和出站流量，以及允许或拒绝流量。 UDR 用于控制虚拟网络、Internet 和本地网络中各资源之间的流量。\r \r > [!WARNING]\r > HDInsight 要求从 Azure 云中的特定 IP 地址进行入站访问，以及进行不受限制的出站访问。 使用 NSG 或 UDR 控制流量时，必须执行以下步骤：\r >\r > 1. 找到虚拟网络所在位置的 IP 地址。 如需按位置列出的必需 IP，请参阅[必需 IP 地址](./hdinsight-extend-hadoop-virtual-network.md#hdinsight-ip)。\r >\r > 2. 允许来自 IP 地址的入站流量。\r >\r >    * NSG：在端口 443 上允许来自 Internet 的入站流量。\r >    * UDR：将路由的“下一跃点”类型设置为“Internet”。\r \r 如需使用 Azure PowerShell 或 Azure CLI 来创建 NSG 的示例，请参阅[使用 Azure 虚拟网络扩展 HDInsight](./hdinsight-extend-hadoop-virtual-network.md#hdinsight-nsg) 文档。\r \r ## <a name=\"create-the-hdinsight-cluster\"></a>创建 HDInsight 群集\r \r > [!WARNING]\r > 必须先配置自定义 DNS 服务器，然后才能将 HDInsight 安装到虚拟网络中。\r \r 通过[使用 Azure 门户创建 HDInsight 群集](./hdinsight-hadoop-create-linux-clusters-portal.md)文档中的步骤创建 HDInsight 群集。\r \r > [!WARNING]\r > * 在群集创建过程中，必须选择虚拟网络所在的位置。\r >\r > * 在配置的“高级设置”部分，必须选择此前创建的虚拟网络和子网。\r \r ## <a name=\"connecting-to-hdinsight\"></a>连接到 HDInsight\r \r HDInsight 上的大多数文档假定你可以通过 Internet 访问群集。 例如，这些文档假定你可以连接到 https://CLUSTERNAME.azurehdinsight.cn 上的群集。 此地址使用公共网关，如果你使用了 NSG 或 UDR 限制来自 Internet 的访问，则该网关不可用。\r 一些文档在通过 SSH 会话连接到群集时还引用了 `headnodehost`。 该地址仅可在群集中的节点上使用，在通过虚拟网络连接的客户端上不可用。\r \r 若要通过虚拟网络直接连接到 HDInsight，请使用以下步骤：\r \r 1. 若要发现 HDInsight 群集节点的内部完全限定的域名，请使用以下方法之一：\r \r     ```powershell\r     $resourceGroupName = \"The resource group that contains the virtual network used with HDInsight\"\r \r     $clusterNICs = Get-AzureRmNetworkInterface -ResourceGroupName $resourceGroupName | where-object {$_.Name -like \"*node*\"}\r \r     $nodes = @()\r     foreach($nic in $clusterNICs) {\r         $node = new-object System.Object\r         $node | add-member -MemberType NoteProperty -name \"Type\" -value $nic.Name.Split('-')[1]\r         $node | add-member -MemberType NoteProperty -name \"InternalIP\" -value $nic.IpConfigurations.PrivateIpAddress\r         $node | add-member -MemberType NoteProperty -name \"InternalFQDN\" -value $nic.DnsSettings.InternalFqdn\r         $nodes += $node\r     }\r     $nodes | sort-object Type\r     ```\r \r     ```azurecli\r     az network nic list --resource-group <resourcegroupname> --output table --query \"[?contains(name,'node')].{NICname:name,InternalIP:ipConfigurations[0].privateIpAddress,InternalFQDN:dnsSettings.internalFqdn}\"\r     ```\r \r 2. 若要确定在其上提供服务的端口，请参阅[由 HDInsight 上的 Hadoop 服务使用的端口](./hdinsight-hadoop-port-settings-for-services.md)文档。\r \r     > [!IMPORTANT]\r     > 托管在头节点上的某些服务一次只能在一个节点上处于活动状态。 如果在一个头节点上尝试访问服务并失败，请切换到其他头节点。\r     >\r     > 例如，Ambari 一次仅在一个头节点上处于活动状态。 如果在一个头节点上尝试访问 Ambari 并返回 404 错误，则它正在其他头节点上运行。\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r * 若要详细了解如何在虚拟网络中使用 HDInsight，请参阅[使用 Azure 虚拟网络扩展 HDInsight](./hdinsight-extend-hadoop-virtual-network.md)。\r \r * 有关 Azure 虚拟网络的详细信息，请参阅 [Azure 虚拟网络概述](../virtual-network/virtual-networks-overview.md)。\r \r * 有关网络安全组的详细信息，请参阅[网络安全组](../virtual-network/virtual-networks-nsg.md)。\r \r * 有关用户定义路由的详细信息，请参阅[用户定义路由和 IP 转发](../virtual-network/virtual-networks-udr-overview.md)。\r <!--Update_Description: update wording and link references-->"}