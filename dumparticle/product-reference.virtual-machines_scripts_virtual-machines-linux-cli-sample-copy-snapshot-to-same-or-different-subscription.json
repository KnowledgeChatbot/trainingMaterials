{"Title":"使用 CLI 将托管磁盘的快照复制到相同或不同的订阅","Description":"Azure CLI 脚本示例 - 使用 CLI 将托管磁盘的快照复制（移动）到相同或不同的订阅","Content":"# <a name=\"copy-snapshot-of-a-managed-disk-to-same-or-different-subscription-with-cli\"></a>使用 CLI 将托管磁盘的快照复制到相同或不同的订阅\r \r 此脚本会将托管磁盘的快照复制到相同或不同的订阅。 使用此脚本将快照移到父快照所在区域中的不同订阅。\r \r [!INCLUDE [sample-cli-install](../../../includes/sample-cli-install.md)]\r \r [!INCLUDE [quickstarts-free-trial-note](../../../includes/quickstarts-free-trial-note.md)]\r \r ## <a name=\"sample-script\"></a>示例脚本\r \r ```azurecli\r #Provide the subscription Id of the subscription where snapshot exists\r sourceSubscriptionId=<your_subscription_id>\r \r #Provide the name of your resource group where snapshot exists\r sourceResourceGroupName=mySourceResourceGroupName\r \r #Provide the name of the snapshot\r snapshotName=mySnapshotName\r \r #Set the context to the subscription Id where snapshot exists\r az account set --subscription $sourceSubscriptionId\r \r #Get the snapshot Id \r snapshotId=$(az snapshot show --name $snapshotName --resource-group $sourceResourceGroupName --query [id] -o tsv)\r \r #If snapshotId is blank then it means that snapshot does not exist.\r echo 'source snapshot Id is: ' $snapshotId\r \r #Provide the subscription Id of the subscription where snapshot will be copied to\r #If snapshot is copied to the same subscription then you can skip this step\r targetSubscriptionId=<target_subscription_id>\r \r #Name of the resource group where snapshot will be copied to\r targetResourceGroupName=mytargetResourceGroupName\r \r #Set the context to the subscription Id where snapshot will be copied to\r #If snapshot is copied to the same subscription then you can skip this step\r az account set --subscription $targetSubscriptionId\r \r #Copy snapshot to different subscription using the snapshot Id\r az snapshot create --resource-group $targetResourceGroupName --name $snapshotName --source $snapshotId\r \r ```\r \r ## <a name=\"script-explanation\"></a>脚本说明\r \r 此脚本使用以下命令，通过源快照的 ID 在目标订阅中创建快照。 表中的每条命令均链接到特定于命令的文档。\r \r | 命令 | 说明 |\r |---|---|\r | [az snapshot show](https://docs.azure.cn/zh-cn/cli/snapshot?view=azure-cli-latest#az_snapshot_show) | 使用快照的名称和资源组属性获取该快照的所有属性。 使用 ID 属性将快照复制到其他订阅。  |\r | [az snapshot create](https://docs.azure.cn/zh-cn/cli/snapshot?view=azure-cli-latest#az_snapshot_create) | 通过使用父快照的 ID 和名称在不同订阅中创建快照来复制快照。  |\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r [从快照创建虚拟机](./virtual-machines-linux-cli-sample-create-vm-from-snapshot.md?toc=%2fpowershell%2fmodule%2ftoc.json)\r \r 有关 Azure CLI 的详细信息，请参阅 [Azure CLI 文档](https://docs.azure.cn/zh-cn/cli/overview?view=azure-cli-latest)。\r \r 可以在 [Azure Linux VM 文档](../../app-service/app-service-cli-samples.md?toc=%2fvirtual-machines%2flinux%2ftoc.json)中找到其他虚拟机和托管磁盘 CLI 脚本示例。\r \r <!--Update_Description: update meta properties, update link-->"}