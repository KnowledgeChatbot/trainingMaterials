{"Title":"Azure PowerShell 脚本示例 - 轮换存储帐户访问密钥","Description":"创建 Azure 存储帐户，然后检索并轮换其中的一个帐户访问密钥。","Content":"# <a name=\"create-a-storage-account-and-rotate-its-account-access-keys\"></a>创建存储帐户并轮换其帐户访问密钥\r \r 此脚本会创建一个 Azure 存储帐户，显示新存储帐户的主访问密钥，然后续订（轮换）密钥。\r \r [!INCLUDE [sample-powershell-install](../../../includes/sample-powershell-install-no-ssh.md)]\r \r [!INCLUDE [quickstarts-free-trial-note](../../../includes/quickstarts-free-trial-note.md)]\r \r ## <a name=\"sample-script\"></a>示例脚本\r \r ```powershell\r # this script will show how to rotate one of the access keys for a storage account\r \r # get list of locations and pick one\r Get-AzureRmLocation | select Location\r \r # save the location you want to use  \r $location = \"China East\"\r \r # create a resource group\r $resourceGroup = \"rotatekeystestrg\"\r New-AzureRmResourceGroup -Name $resourceGroup -Location $location \r \r # create a standard general-purpose storage account \r $storageAccountName = \"contosotestkeys\"\r New-AzureRmStorageAccount -ResourceGroupName $resourceGroup `\r   -Name $storageAccountName `\r   -Location $location `\r   -SkuName Standard_LRS `\r \r # retrieve the first storage account key and display it \r $storageAccountKey = (Get-AzureRmStorageAccountKey -ResourceGroupName $resourceGroup -Name $storageAccountName).Value[0]\r \r Write-Host \"storage account key 1 = \" $storageAccountKey\r \r # re-generate the key\r New-AzureRmStorageAccountKey -ResourceGroupName $resourceGroup `\r     -Name $storageAccountName `\r     -KeyName key1\r \r # retrieve it again and display it \r $storageAccountKey = (Get-AzureRmStorageAccountKey -ResourceGroupName $resourceGroup -Name $storageAccountName).Value[0]\r Write-Host \"storage account key 1 = \" $storageAccountKey\r ```\r \r ## <a name=\"clean-up-deployment\"></a>清理部署 \r \r 运行以下命令来删除资源组、存储帐户和所有相关资源。\r \r ```powershell\r Remove-AzureRmResourceGroup -Name rotatekeystestrg\r ```\r \r ## <a name=\"script-explanation\"></a>脚本说明\r \r 此脚本使用以下命令创建存储帐户并检索和轮换其中的一个访问密钥。 表中的每一项均链接到命令特定的文档。\r \r | 命令 | 说明 |\r |---|---|\r | [Get-AzureRmLocation](https://docs.microsoft.com/powershell/module/azurerm.resources/get-azurermlocation) | 获取所有位置以及每个位置支持的资源提供程序。 |\r | [New-AzureRmResourceGroup](https://docs.microsoft.com/powershell/module/azurerm.resources/new-azurermresourcegroup) | 创建 Azure 资源组。 |\r | [New-AzureRmStorageAccount](https://docs.microsoft.com/powershell/module/azurerm.storage/new-azurermstorageaccount) | 创建存储帐户。 |\r | [Get-AzureRmStorageAccountKey](https://docs.microsoft.com/powershell/module/azurerm.storage/get-azurermstorageaccountkey) | 获取 Azure 存储帐户的访问密钥。 |\r | [New-AzureRmStorageAccountKey](https://docs.microsoft.com/powershell/module/azurerm.storage/new-azurermstorageaccountkey) | 重新生成 Azure 存储帐户的访问密钥。 |\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r 有关 Azure PowerShell 模块的详细信息，请参阅 [Azure PowerShell 文档](https://docs.microsoft.com/powershell/azure/overview)。\r \r 有关其他存储 PowerShell 脚本示例，可参阅 [Azure Blob 存储的 PowerShell 示例](../blobs/storage-samples-blobs-powershell.md)。\r "}