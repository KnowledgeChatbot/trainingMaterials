{"Title":"通过 Azure IoT 中心安排作业 (Node)","Description":"如何安排 Azure IoT 中心作业实现多台设备上的直接方法调用。 使用 Azure IoT SDK for Node.js 实现模拟设备应用以及用于运行作业的服务应用。","Content":"# <a name=\"schedule-and-broadcast-jobs-node\"></a>计划和广播作业 (Node)\r \r [!INCLUDE [iot-hub-selector-schedule-jobs](../../includes/iot-hub-selector-schedule-jobs.md)]\r \r Azure IoT 中心是一项完全托管的服务，允许后端应用创建和跟踪用于计划和更新数百万台设备的作业。  作业可以用于以下操作：\r \r * 更新所需属性\r * 更新标记\r * 调用直接方法\r \r 从概念上讲，作业包装这些操作之一，并跟踪针对一组设备执行（由设备孪生查询定义）的进度。  例如，后端应用可使用作业重启 10,000 台设备（由设备孪生查询指定并计划在将来执行）。  该应用程序随后可以在其中每个设备接收和执行重新启动方法时跟踪进度。\r \r 可在以下文章中了解有关所有这些功能的详细信息：\r \r * 设备孪生和属性：[设备孪生入门][lnk-get-started-twin]和[教程：如何使用设备孪生属性][lnk-twin-props]\r * 直接方法：[IoT 中心开发人员指南 - 直接方法][lnk-dev-methods]和[教程：直接方法][lnk-c2d-methods]\r \r 本教程演示如何：\r \r * 创建一个具有直接方法的 Node.js 模拟设备应用，启用可由解决方案后端进行调用的 lockDoor。\r * 创建一个 Node.js 控制台应用，该应用使用作业调用模拟设备应用中的 **lockDoor** 直接方法，并使用设备作业更新所需属性。\r \r 在本教程结束时，会创建两个 Node.js 应用：\r \r **simDevice.js**，它使用设备标识连接到 IoT 中心，并接收 **lockDoor** 直接方法。\r \r scheduleJobService.js，它调用模拟设备应用中的直接方法，并通过作业更新设备孪生的所需属性。\r \r 要完成本教程，需要以下各项：\r \r * Node.js 版本 4.0.x 或更高版本； <br/>  [准备开发环境][lnk-dev-setup]介绍了如何在 Windows 或 Linux 上安装本教程所用的 Node.js。\r * 有效的 Azure 帐户。 如果没有帐户，可以创建一个[试用帐户][lnk-free-trial]，只需几分钟即可完成。\r \r [!INCLUDE [iot-hub-get-started-create-hub](../../includes/iot-hub-get-started-create-hub.md)]\r \r [!INCLUDE [iot-hub-get-started-create-device-identity](../../includes/iot-hub-get-started-create-device-identity.md)]\r \r ## <a name=\"create-a-simulated-device-app\"></a>创建模拟设备应用程序\r 本部分将创建一个 Node.js 控制台应用，用于响应通过云调用的方法，这会触发模拟 lockDoor 方法。\r \r 1. 新建名为 **simDevice**的空文件夹。  在 **simDevice** 文件夹的命令提示符处，使用以下命令创建 package.json 文件。  接受所有默认值：\r \r     ```\r     npm init\r     ```\r 2. 在 **simDevice** 文件夹的命令提示符处，运行下述命令以安装 **azure-iot-device** 设备 SDK 包和 **azure-iot-device-mqtt** 包：\r \r     ```\r     npm install azure-iot-device azure-iot-device-mqtt --save\r     ```\r 3. 在 **simDevice.js** 文件夹中，利用文本编辑器创建新的 **simDevice** 文件。\r 4. 在 **simDevice.js** 文件的开头添加以下“require”语句：\r \r     ```\r     'use strict';\r \r     var Client = require('azure-iot-device').Client;\r     var Protocol = require('azure-iot-device-mqtt').Mqtt;\r     ```\r 5. 添加 **connectionString** 变量，并使用它创建一个**客户端**实例。  \r \r     ```\r     var connectionString = 'HostName={youriothostname};DeviceId={yourdeviceid};SharedAccessKey={yourdevicekey}';\r     var client = Client.fromConnectionString(connectionString, Protocol);\r     ```\r 6. 添加以下函数以处理 **lockDoor** 方法。\r \r     ```\r     var onLockDoor = function(request, response) {\r \r         // Respond the cloud app for the direct method\r         response.send(200, function(err) {\r             if (!err) {\r                 console.error('An error occured when sending a method response:\\n' + err.toString());\r             } else {\r                 console.log('Response to method \\'' + request.methodName + '\\' sent successfully.');\r             }\r         });\r \r         console.log('Locking Door!');\r     };\r     ```\r 7. 添加以下代码以注册 **lockDoor** 方法的处理程序。\r \r     ```\r     client.open(function(err) {\r         if (err) {\r             console.error('Could not connect to IotHub client.');\r         }  else {\r             console.log('Client connected to IoT Hub. Register handler for lockDoor direct method.');\r             client.onDeviceMethod('lockDoor', onLockDoor);\r         }\r     });\r     ```\r 8. 保存并关闭 **simDevice.js** 文件。\r \r > [!NOTE]\r > 为简单起见，本教程不实现任何重试策略。 在生产代码中，应该按 MSDN 文章 [Transient Fault Handling][lnk-transient-faults]（暂时性故障处理）中所述实施重试策略（例如指数性的回退）。\r > \r > \r \r ## <a name=\"schedule-jobs-for-calling-a-direct-method-and-updating-a-device-twins-properties\"></a>安排作业，用于调用直接方法和更新设备孪生的属性\r 在此部分中，会创建一个 Node.js 控制台应用，它使用直接方法对设备启动远程 **lockDoor** 并更新设备孪生的属性。\r \r 1. 新建名为 **scheduleJobService**的空文件夹。  在 **scheduleJobService** 文件夹的命令提示符处，使用以下命令创建 package.json 文件。  接受所有默认值：\r \r     ```\r     npm init\r     ```\r 2. 在 scheduleJobService 文件夹的命令提示符处，运行以下命令安装 azure-iothub 设备 SDK 包和 azure-iot-device-mqtt 包：\r    \r     ```\r     npm install azure-iothub uuid --save\r     ```\r 3. 在 **scheduleJobService** 文件夹中，利用文本编辑器创建新的 **scheduleJobService.js** 文件。\r 4. 在 **dmpatterns_gscheduleJobServiceetstarted_service.js** 文件开头添加以下“require”语句：\r \r     ```\r     'use strict';\r \r     var uuid = require('uuid');\r     var JobClient = require('azure-iothub').JobClient;\r     ```\r 5. 添加以下变量声明并替换占位符值：\r \r     ```\r     var connectionString = '{iothubconnectionstring}';\r     var queryCondition = \"deviceId IN ['myDeviceId']\";\r     var startTime = new Date();\r     var maxExecutionTimeInSeconds =  300;\r     var jobClient = JobClient.fromConnectionString(connectionString);\r     ```\r 6. 添加以下用于监视作业执行的函数：\r    \r     ```\r     function monitorJob (jobId, callback) {\r         var jobMonitorInterval = setInterval(function() {\r             jobClient.getJob(jobId, function(err, result) {\r             if (err) {\r                 console.error('Could not get job status: ' + err.message);\r             } else {\r                 console.log('Job: ' + jobId + ' - status: ' + result.status);\r                 if (result.status === 'completed' || result.status === 'failed' || result.status === 'cancelled') {\r                 clearInterval(jobMonitorInterval);\r                 callback(null, result);\r                 }\r             }\r             });\r         }, 5000);\r     }\r     ```\r 7. 添加以下代码以安排调用设备方法的作业：\r \r     ```\r     var methodParams = {\r         methodName: 'lockDoor',\r         payload: null,\r         responseTimeoutInSeconds: 15 // Timeout after 15 seconds if device is unable to process method\r     };\r \r     var methodJobId = uuid.v4();\r     console.log('scheduling Device Method job with id: ' + methodJobId);\r     jobClient.scheduleDeviceMethod(methodJobId,\r                                 queryCondition,\r                                 methodParams,\r                                 startTime,\r                                 maxExecutionTimeInSeconds,\r                                 function(err) {\r         if (err) {\r             console.error('Could not schedule device method job: ' + err.message);\r         } else {\r             monitorJob(methodJobId, function(err, result) {\r                 if (err) {\r                     console.error('Could not monitor device method job: ' + err.message);\r                 } else {\r                     console.log(JSON.stringify(result, null, 2));\r                 }\r             });\r         }\r     });\r     ```\r 8. 添加以下代码以安排更新设备孪生的作业：\r    \r     ```\r     var twinPatch = {\r        etag: '*', \r        properties: {\r            desired: {\r                building: '43', \r                floor: 3\r            }\r        }\r     };\r \r     var twinJobId = uuid.v4();\r \r     console.log('scheduling Twin Update job with id: ' + twinJobId);\r     jobClient.scheduleTwinUpdate(twinJobId,\r                                 queryCondition,\r                                 twinPatch,\r                                 startTime,\r                                 maxExecutionTimeInSeconds,\r                                 function(err) {\r         if (err) {\r             console.error('Could not schedule twin update job: ' + err.message);\r         } else {\r             monitorJob(twinJobId, function(err, result) {\r                 if (err) {\r                     console.error('Could not monitor twin update job: ' + err.message);\r                 } else {\r                     console.log(JSON.stringify(result, null, 2));\r                 }\r             });\r         }\r     });\r     ```\r 9. 保存并关闭 **scheduleJobService.js** 文件。\r \r ## <a name=\"run-the-applications\"></a>运行应用程序\r 现在，已准备就绪，可以运行应用程序了。\r \r 1. 在 simDevice 文件夹的命令提示符处，运行以下命令以开始侦听重启直接方法。\r    \r     ```\r     node simDevice.js\r     ```\r 2. 在 **scheduleJobService** 文件夹的命令提示符处运行以下命令，以便触发作业进行锁门和孪生项的更新\r \r     ```\r     node scheduleJobService.js\r     ```\r 3. 可以在控制台中看到设备对直接方法的响应。\r \r ## <a name=\"next-steps\"></a>后续步骤\r 在本教程中，使用了作业来安排用于设备的直接方法以及设备孪生属性的更新。\r \r 若要继续完成 IoT 中心和设备管理模式（如远程无线固件更新）的入门内容，请参阅：\r \r [教程：如何进行固件更新][lnk-fwupdate]\r \r 若要继续完成 IoT 中心入门内容，请参阅 [Azure IoT Edge 入门][lnk-iot-edge]。\r \r [lnk-get-started-twin]: ./iot-hub-node-node-twin-getstarted.md\r [lnk-twin-props]: ./iot-hub-node-node-twin-how-to-configure.md\r [lnk-c2d-methods]: ./iot-hub-node-node-direct-methods.md\r [lnk-dev-methods]: ./iot-hub-devguide-direct-methods.md\r [lnk-fwupdate]: ./iot-hub-node-node-firmware-update.md\r [lnk-iot-edge]: ./iot-hub-linux-iot-edge-get-started.md\r [lnk-dev-setup]: https://github.com/Azure/azure-iot-sdk-node/tree/master/doc/node-devbox-setup.md\r [lnk-free-trial]: https://www.azure.cn/pricing/1rmb-trial/\r [lnk-transient-faults]: https://msdn.microsoft.com/zh-cn/library/hh680901(v=pandp.50).aspx\r \r <!--Update_Description:update wording and code-->\r "}