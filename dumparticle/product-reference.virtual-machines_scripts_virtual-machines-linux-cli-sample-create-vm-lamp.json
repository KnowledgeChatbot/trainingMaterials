{"Title":"在负载均衡虚拟机规模集中部署 LAMP 堆栈","Description":"使用自定义脚本扩展在 Azure 上的负载均衡虚拟机规模集中部署 LAMP 堆栈。","Content":"# <a name=\"deploy-the-lamp-stack-in-a-load-balanced-virtual-machine-scale-set\"></a>在负载均衡虚拟机规模集中部署 LAMP 堆栈\r \r 此示例创建一个虚拟机规模集，并应用运行自定义脚本的扩展在规模集中每个虚拟机上部署 LAMP 堆栈。\r \r [!INCLUDE [sample-cli-install](../../../includes/sample-cli-install.md)]\r \r ## <a name=\"sample-script\"></a>示例脚本\r \r ```azurecli\r #!/bin/bash\r \r # Create the resource group if it doesn't exist\r az group create -n myResourceGroup -l chinanorth\r \r # Build the Scale Set\r az vmss create -n myScaleSet -g myResourceGroup --public-ip-address-dns-name my-lamp-sample \\\r     --image CentOS --storage-sku Premium_LRS --admin-username deploy --vm-sku Standard_DS3_v2\r \r # Add a load balanced endpoint on port 80 routed to the backend servers on port 80\r az network lb rule create -g myResourceGroup -n http-rule --backend-pool-name myScaleSetLBBEPool \\\r     --backend-port 80 --frontend-ip-name LoadBalancerFrontEnd --frontend-port 80 --lb-name myScaleSetLB \\\r     --protocol Tcp\r \r # Create a virtual machine scale set custom script extension. This extension will provide configuration\r # to each of the virtual machines within the scale set on how to provision their software stack.\r # The configuration (./projected_config.json) contains commands to be executed upon provisioning\r # of instances. This is helpful for hooking into configuration management software or simply\r # provisioning your software stack directly.\r az vmss extension set -n CustomScript --publisher Microsoft.Azure.Extensions --version 2.0 \\\r    -g myResourceGroup --vmss-name myScaleSet --protected-settings ./protected_config.json --no-auto-upgrade\r \r # The instances that we have were created before the extension was added.\r # Update these instances to run the new configuration on each of them.\r az vmss update-instances --instance-ids \"*\" -n myScaleSet -g myResourceGroup\r \r # Scaling adds new instances. These instances will run the configuration when they're provisioned.\r az vmss scale --new-capacity 2 -n myScaleSet -g myResourceGroup\r \r ```\r \r ## <a name=\"connect\"></a>连接\r \r 使用此代码查看如何连接到 VM 和规模集。\r \r ```azurecli\r #!/bin/bash\r \r FQDN=$(az network public-ip list -g myResourceGroup --query \\\r                 \"[?starts_with(dnsSettings.fqdn, 'my-lamp-')].dnsSettings.fqdn | [0]\" -o tsv)\r \r PORTS=$(az network lb show -g myResourceGroup -n myScaleSetLB \\\r     --query \"inboundNatRules[].{backend: backendPort, frontendPort: frontendPort}\" -o tsv)\r while read CMD; do\r     read _ frontend <<< \"${CMD}\"\r     echo \"'ssh deploy@${FQDN} -p ${frontend}'\"\r done <<< \"${PORTS}\"\r \r echo \"\"\r echo \"You can now reach the scale set by opening your browser to: 'http://${FQDN}'.\"\r ```\r \r ## <a name=\"clean-up-deployment\"></a>清理部署 \r \r 运行如下命令来删除资源组、规模集和 VM 以及所有相关资源。\r \r ```azurecli \r az group delete -n myResourceGroup\r ```\r \r ## <a name=\"script-explanation\"></a>脚本说明\r \r 此脚本使用以下命令创建资源组、虚拟机、可用性集、负载均衡器和所有相关资源。 表中的每条命令均链接到特定于命令的文档。\r \r | 命令 | 说明 |\r |---|---|\r | [az group create](https://docs.azure.cn/zh-cn/cli/group?view=azure-cli-latest#az_group_create) | 创建用于存储所有资源的资源组。 |\r | [az vmss create](https://docs.azure.cn/zh-cn/cli/vmss?view=azure-cli-latest#az_vmss_create) | 创建虚拟机规模集 |\r | [az network lb rule create](https://docs.azure.cn/zh-cn/cli/network/lb/rule?view=azure-cli-latest#az_network_lb_rule_create) | 添加负载均衡终结点 |\r | [az vmss extension set](https://docs.azure.cn/zh-cn/cli/vmss/extension?view=azure-cli-latest#az_vmss_extension_set) | 创建对 VM 部署运行自定义脚本的扩展 |\r | [az vmss update-instances](https://docs.azure.cn/zh-cn/cli/vmss?view=azure-cli-latest#az_vmss_update_instances) | 在将扩展应用到规模集之前部署的 VM 实例上运行此自定义脚本。 |\r | [az vmss scale](https://docs.azure.cn/zh-cn/cli/vmss?view=azure-cli-latest#az_vmss_scale) | 通过添加更多 VM 实例来扩大规模集。 部署实例时，在实例上运行此自定义脚本。 |\r | [az network public-ip list](https://docs.azure.cn/zh-cn/cli/network/public-ip?view=azure-cli-latest#az_network_public_ip_list) | 获取示例创建的 VM 的 IP 地址。 |\r | [az network lb show](https://docs.azure.cn/zh-cn/cli/network/lb?view=azure-cli-latest#az_network_lb_show) | 获取负载均衡器使用的前端和后端端口。 |\r \r ## <a name=\"next-steps\"></a>后续步骤\r \r 有关 Azure CLI 的详细信息，请参阅 [Azure CLI 文档](https://docs.azure.cn/zh-cn/cli/overview?view=azure-cli-latest)。\r \r 可以在 [Azure Linux VM 文档](../linux/cli-samples.md?toc=%2fvirtual-machines%2flinux%2ftoc.json)中找到其他虚拟机 CLI 脚本示例。\r \r <!--Update_Description: update link-->"}