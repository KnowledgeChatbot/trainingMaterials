{"Title":"获取应用身份验证的值 - Azure SQL 数据库","Description":"创建服务主体以便从代码访问 SQL 数据库。","Content":"# <a name=\"get-the-required-values-for-authenticating-an-application-to-access-sql-database-from-code\"></a>获取对应用程序进行身份验证所需的值以便从代码访问 SQL 数据库\r 若要在代码中创建并管理 SQL 数据库，必须在创建 Azure 资源的订阅中的 Azure Active Directory (AAD) 域内注册你的应用。\r \r ## <a name=\"create-a-service-principal-to-access-resources-from-an-application\"></a>创建服务主体以便从应用程序访问资源\r \r 需要安装并运行最新的 [Azure PowerShell](https://msdn.microsoft.com/library/mt619274.aspx) 。 有关详细信息，请参阅 [如何安装和配置 Azure PowerShell](../powershell-install-configure.md)。\r \r 以下 PowerShell 脚本将创建对 C# 应用进行身份验证所需的 Active Directory (AD) 应用程序和服务主体。 该脚本输出我们需要用于前面 C# 示例的值。 有关详细信息，请参阅[使用 Azure PowerShell 创建服务主体以访问资源](../azure-resource-manager/resource-group-authenticate-service-principal.md)。\r \r ```\r # Sign in to Azure.\r Add-AzureRmAccount -EnvironmentName AzureChinaCloud\r \r # If you have multiple subscriptions, uncomment and set to the subscription you want to work with.\r #$subscriptionId = \"{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}\"\r #Set-AzureRmContext -SubscriptionId $subscriptionId\r \r # Provide these values for your new AAD app.\r # $appName is the display name for your app, must be unique in your directory.\r # $uri does not need to be a real uri.\r # $secret is a password you create.\r \r $appName = \"{app-name}\"\r $uri = \"http://{app-name}\"\r $secret = \"{app-password}\"\r \r # Create a AAD app\r $azureAdApplication = New-AzureRmADApplication -DisplayName $appName -HomePage $Uri -IdentifierUris $Uri -Password $secret\r \r # Create a Service Principal for the app\r $svcprincipal = New-AzureRmADServicePrincipal -ApplicationId $azureAdApplication.ApplicationId\r \r # To avoid a PrincipalNotFound error, I pause here for 15 seconds.\r Start-Sleep -s 15\r \r # If you still get a PrincipalNotFound error, then rerun the following until successful. \r $roleassignment = New-AzureRmRoleAssignment -RoleDefinitionName Contributor -ServicePrincipalName $azureAdApplication.ApplicationId.Guid\r \r # Output the values we need for our C# application to successfully authenticate\r \r Write-Output \"Copy these values into the C# sample app\"\r \r Write-Output \"_subscriptionId:\" (Get-AzureRmContext).Subscription.SubscriptionId\r Write-Output \"_tenantId:\" (Get-AzureRmContext).Tenant.TenantId\r Write-Output \"_applicationId:\" $azureAdApplication.ApplicationId.Guid\r Write-Output \"_applicationSecret:\" $secret\r ```\r \r ## <a name=\"see-also\"></a>另请参阅\r \r - [使用 C# 创建 SQL 数据库](sql-database-get-started-csharp.md)\r - [使用 Azure Active Directory 身份验证连接到 SQL 数据库](sql-database-aad-authentication.md)\r \r <!--Update_Description: update meta properties-->"}